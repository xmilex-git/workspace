.claude/locale/*.so
.git_ignored_dir/
porting_plan.md

# Local environment (real addresses / IPs / paths / tokens) — never commit
.envrc
.envrc.local
.not_git_tracking
.gjc
docs/agents
.claude/scratch
