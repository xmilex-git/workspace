# PR 4794: [CBRD-24914] Core dumped in pt_ordbynum_to_key_limit_multiple_ranges at src/parser/xasl_generation.c:23489

- state: MERGED / base: develop / created: 2023-11-20
- diff: +68 -0, 2 files

## Code changes (summary)

- `qo_reduce_equality_terms()` (`query_rewrite.c`): added a new pass over the CNF term list that detects "always-true" equality terms — a single (non-OR-chained) term of the form `arg1 = arg2` where both operands are `PT_VALUE` nodes and `db_value_compare()` returns `DB_EQ` — and splices such terms out of the linked list, freeing them
- `qo_check_terms_for_multiple_range_opt()` (`plan_generation.c`): added a check that bails out of Multiple Range Optimization (returns `NO_ERROR` / gives up) when a term's `segments` bitset is empty, instead of proceeding into logic that previously asserted on always-true/false conditions
- New local variables (`cut_off`, `expr_prev`, `opd1`, `opd2`, `dbv1`, `dbv2`) added to support the term-removal walk

## PR body (verbatim)

http://jira.cubrid.org/browse/CBRD-24914

When using `MRO optimization`, we fixed an issue where a condition created by rewrite that is `always true/false` was recognized as a data filter and asserted. 

- The following are the revised contents: 

1. We modified the `qo_reduce_equality_terms()` function to remove expressions found to be `always true` before the function ends. 

3. In `qo_check_terms_for_multiple_range_opt()`, if a term is identified as `PT_VALUE`, it is treated as `always false` since the `always true` terms have been removed, thus preventing `MRO optimization`.

