# PR 6225: [CBRD-26090] [Regression][shell] Core dumped in src/query/parallel/px_heap_scan/px_heap_scan_manager.cpp:392

- state: MERGED / base: develop / created: 2025-05-30
- diff: +2 -1, 1 files

## Code changes (summary)

- `src/query/parallel/px_heap_scan/px_heap_scan_task.cpp`: `task::retire()`에서 `cubthread::entry_task::retire()` 호출 전에 `m_worker_manager` 포인터를 로컬 변수(`worker_manager_p`)에 저장
- `cubthread::entry_task::retire()`가 `this`(task 객체)를 삭제할 수 있어, 그 이후에 `m_worker_manager`를 참조하면 free된 메모리에 접근하는 문제가 있었음 — 로컬 변수로 미리 캡처해서 해결
- 직전 PR(#6213)에서 추가된 코드가 유발한 회귀를 수정

## PR body (verbatim)

<http://jira.cubrid.org/browse/CBRD-26090>

### Purpose

지난 PR(#6213)에서 free된 메모리에 접근하는 코드를 추가하였습니다.
이를 수정합니다.
