# PR 6220: [CBRD-26095] Insert select 문의 parallel heap scan 지원

- state: MERGED / base: develop / created: 2025-05-29
- diff: +1 -1, 1 files

## Code changes (summary)

- `src/query/parallel/px_heap_scan/px_heap_scan_checker.cpp`: `INSERT_PROC`를 parallel 불가 case 목록에서 제거하고 no-op(`break`) case 목록으로 이동
- INSERT SELECT 문에 대해서도 parallel heap scan 적용 가능 여부 판단(`scan_check_parallel_heap_scan_possible`)에서 더 이상 무조건 CANNOT_PARALLEL로 처리하지 않도록 제약 해제

## PR body (verbatim)

<http://jira.cubrid.org/browse/CBRD-26095>

### Purpose

현재 CUBRID의 Parallel Heap Scan 기능은 Insert select문에 대해 적용되지 않습니다.
본 개선은 Insert select문에 대해서도 Parallel Heap Scan이 적용될 수 있도록 하여, 대용량 데이터 처리 성능을 향상시키는 것을 목표로 합니다.

### Implementation

scan_check_parallel_heap_scan_possible에 insert 문 제약을 해제합니다.
