# PR 6682: [CBRD-26413] [Regression] Core dumped in locator_attribute_info_force at src/transaction/locator_sr.c:7514

- state: MERGED / base: develop / created: 2025-12-02
- diff: +2 -1, 2 files

## Code changes (summary)

- `query_executor.c`의 `qexec_execute_mainblock_internal()`에서 memoize storage 생성 조건에 `!mvcc_select_lock_needed`를 추가해, select 대상 테이블에 lock이 필요한 경우 memoize를 비활성화.
- `.github/CODEOWNERS`에 `src/query/memoize.*` 항목을 추가해 담당자를 지정.

## PR body (verbatim)

<http://jira.cubrid.org/browse/CBRD-26413>

### Purpose

select 대상에 lock이 필요한 테이블에 대하여, memoize를 비활성화한다.
