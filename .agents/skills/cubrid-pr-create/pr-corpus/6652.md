# PR 6652: [CBRD-26345] Implement memoize

- state: MERGED / base: develop / created: 2025-11-19
- diff: +94 -121, 6 files

## Code changes (summary)

- `plan_generation.c`(-42): outer join에서 ndv 기반으로 `join_outer_table_terms_duplicate_ratio`를 계산해 `XASL_MEMOIZE_STORAGE` 플래그를 세팅하던 로직 전체 제거 (`MEMOIZE_NDV_RATIO_THRESHOLD` 상수 포함)
- `memoize.cpp`(+77-51): memoize storage용 커스텀 `pvector`/`allocator` 사용을 표준 `std::vector`로 교체, `key`/`value` 생성자에서 allocator 인자 제거
- `memoize.cpp`: storage 할당을 `db_private_alloc()`에서 `malloc()`으로 변경 (parallel aptr 등에서 clear 시 trace 미노출 및 런타임 해제 부하 회피)
- `memoize.cpp/hpp`(+11-21): storage clear 시점을 query 종료 시점으로 변경 (storage full 시에는 clear하지 않음)
- `memoize.cpp`: ndv 기반 차단 대신, 1000회 수행 후 hit ratio가 50% 미달이면 memoize를 block하도록 로직 변경 (join 테이블 3개 이상 시 예상 중복률 산출 불가 대응)
- `query_dump.c`(+4-2), `query_executor.c`(-4), `xasl.h`(-1): 제거된 플래그/필드에 따른 연동 정리

## PR body (verbatim)

<http://jira.cubrid.org/browse/CBRD-26345>

### Purpose

3가지 수정을 진행합니다.

1. memoize storage clear를 query 종료 시점으로 변경 (storage full 시 clear하지 않음)
2. memoize storage를 malloc()으로 할당하게 변경 (parallel aptr 등에서 clear 시 trace가 나타나지 않고, runtime에 할당해제하는 부하가 추가되기 때문)
3. memoize를 ndv로 막지 않고, 1000번 수행하여 hit ratio가 50%가 미달될 시 block하게 변경 (join 테이블 수가 3개 이상일 경우, 현재 예상 중복률을 구할 수 없기 때문)
