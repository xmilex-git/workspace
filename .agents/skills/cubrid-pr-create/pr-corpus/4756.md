# PR 4756: [CBRD-25048] Core dumped in pgbuf_replace_watcher_debug at src/storage/page_buffer.c:12574

- state: MERGED / base: develop / created: 2023-11-02
- diff: +4 -1, 1 files

## Code changes (summary)

- `heap_next_internal()` (`heap_file.c`): added a new local `rec_info_page_watcher` variable
- When the `select_record_info` hint path is taken (`get_rec_info`), the code now initializes `rec_info_page_watcher` and replaces `scan_cache->page_watcher` into it via `pgbuf_replace_watcher()` before calling `heap_get_record_info()`, passing `&rec_info_page_watcher` instead of `&scan_cache->page_watcher`
- Restores the pre-CBRD-25037 watcher-copy semantics specifically for the `get_rec_info` path, fixing a crash caused by that PR's removal of the local watcher

## PR body (verbatim)

http://jira.cubrid.org/browse/CBRD-25048

This issue arises due to the modifications induced by the CBRD-25037 issue. The reason for the error is that the processing within the `heap_next_internal ()` differs between when the `select_record_info` hint is used and when it is not used. Therefore, when the `select_record_info` hint is used, modifications have been made to transfer the value to the local page_watcher variable as before the change, and then execute `heap_get_record_info ()`.
