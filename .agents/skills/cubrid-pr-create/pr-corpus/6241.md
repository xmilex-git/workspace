# PR 6241: [CBRD-26114] Partition table에 대한 parallel heap scan trace가 출력되지 않음

- state: MERGED / base: develop / created: 2025-06-10
- diff: +78 -4, 4 files

## Code changes (summary)

- `px_heap_scan_perf_monitor.hpp`/`.cpp`: `set_partition_stats()`, `add_scan_stats()` 신규 메서드 추가, 이전 스캔 통계(`m_prev_scan_stats`)를 저장해 파티션 경계별 증분 통계를 계산
- `perf_monitor` 생성자에서 `m_prev_scan_stats` 초기화 로직 추가; `mergable`/`mergeable` 오타 수정 (`mergable list` → `mergeable list`, text/json 출력 양쪽)
- `query_dump.c`: `qdump_print_access_spec_stats_text()`에서 `S_HEAP_SCAN` 타입(파티션 마지막 스캔에서 heap scan으로 전환된 경우)도 `S_PARALLEL_HEAP_SCAN`과 동일하게 perf_monitor 존재 여부를 확인하고 병합 리스트 통계를 누적하도록 조건 확장
- `query_executor.c`: `qexec_open_scan()`에서 parallel heap scan 판정 실패 시 `ACCESS_SPEC_FLAG_NO_PARALLEL_HEAP_SCAN` 플래그를 설정하는 else 분기 추가; `qexec_init_next_partition()`에서 파티션 전환 직전의 `prev_partition_spec`을 캡처해 `perf_monitor->set_partition_stats()`로 파티션별 통계를 확정
- 파티션 스캔의 경우 마지막 파티션에서 스캔 타입이 `S_HEAP_SCAN`으로 바뀌어 parallel heap scan trace가 출력되지 않던 문제를 해결

## PR body (verbatim)

<http://jira.cubrid.org/browse/CBRD-26114>

### Purpose

 현재 CUBRID에서는 파티션에 대한 힙 스캔을 수행할 때, parallel heap scan이 실행되었음에도 불구하고 해당 작업에 대한 trace 정보가 출력되지 않는 문제가 있습니다. 이 문제를 해결합니다.

### Implementation

파티션 스캔의 경우 마지막 partitioned_table에 대한 open_scan 시 S_HEAP_SCAN으로 변경되기 때문에, S_HEAP_SCAN인 경우에도 perf_monitor가 있는지 확인하고 출력합니다.

### Remarks

SCAN_ID는 sizeof(SCAN_ID) 만큼 memset(0)을 수행하기 때문에, 일반 힙 스캔의 경우 perf_monitor는 nullptr이여야 합니다.
