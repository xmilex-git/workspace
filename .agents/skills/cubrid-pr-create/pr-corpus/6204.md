# PR 6204: [CBRD-26087] [Regression] Core dumped in qfile_reopen_list_as_append_mode at src/query/list_file.c:1348

- state: MERGED / base: develop / created: 2025-05-22
- diff: +5 -1, 1 files

## Code changes (summary)

- `px_heap_scan_manager.cpp` (`scan_open_parallel_heap_scan`): guard the `qfile_reopen_list_as_append_mode()` call so it only runs when the result-gather method is `LIST_MERGE` and `xasl->list_id` has a valid `tfile_vfid` and a non-null `first_vpid`.
- Fixes a debug-mode core dump (`assert(0)` in `qfile_reopen_list_as_append_mode`, `list_file.c:1348`) that occurred when `list_id` was not yet initialized.

## PR body (verbatim)

<http://jira.cubrid.org/browse/CBRD-XXXX>

### Purpose

reopen() 함수 내부에 list-id가 초기화되지 않은 경우 assert(0) 등이 발생해 debug 모드에서 core dump가 발생합니다. 이를 수정합니다.

### Implementation

list_id가 초기화 되지 않거나, mergable-list 방식이 아닌 경우 reopen 하지 않도록 변경합니다.

### Remarks

N/A

