# PR 5716: [CBRD-25447] Support parallel heap scan

- state: CLOSED / base: develop / created: 2024-12-13
- diff: +3149 -6, 35 files

## Code changes (summary)

(summary based on diffstat; PR body was only "Draft" and diff is too large to read verbatim)

- Adds an entirely new `src/query/parallel_query/parallel_heap_scan/` module (10 new .cpp/.hpp file pairs, largest additions: `phs_memory_mapper.cpp` +584, `phs_misc.cpp` +491, `phs_checker.cpp` +464, `phs_manager.cpp` +256) implementing a parallel heap-scan subsystem with a manager, task/result-queue, context, checker, and memory-mapper components.
- Build system wiring: `CMakeLists.txt`, `cs/CMakeLists.txt` (+18), `cubrid/CMakeLists.txt` (+19), `sa/CMakeLists.txt` (+23), and `.github/CODEOWNERS` updated to compile/own the new parallel_heap_scan sources for CS/SA/cubrid targets.
- `src/base/system_parameter.c`/`.h`: new server system parameter(s) added (+20/+1) to configure/enable parallel heap scan.
- Parser changes: `src/parser/csql_grammar.y`, `name_resolution.c` (+12), `parse_tree.h` (+3/-1), `parse_tree_cl.c` (+5), `scanner_support.c` (+6), `xasl_generation.c` (+13/-1) — add grammar/parse-tree support and hook parallel-scan XASL generation.
- Query engine integration: `src/query/query_dump.c` (+5), `query_executor.c` (+40), `query_reevaluation.cpp` (+3), `scan_manager.c`/`.h` (+35/-2, +30), `xasl.h` (+2/-1) wire the new parallel heap scan path into scan/XASL execution and trace dumping.
- Storage layer: `src/storage/heap_file.c` (+183), `heap_file.h` (+2) add heap-level support functions consumed by the parallel scan manager/checker.

## PR body (verbatim)

http://jira.cubrid.org/browse/CBRD-25447

Draft
