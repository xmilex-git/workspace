# PR 6593: [CBRD-26365] [Regression] Core dumped in cub_free at src/base/memory_cwrapper.h:85

- state: MERGED / base: develop / created: 2025-11-03
- diff: +13 -16, 1 files

## Code changes (summary)

- `px_heap_scan_result_handler.cpp`: destroy 단계에서 writer_results의 list_id들을 `qfile_destroy_list()`로 정리하고 `m_.writer_results.clear()`를 수행하도록 이동 (child thread 종료 전 main thread가 자료구조를 먼저 해제하던 문제 수정)
- `tpl_descr.f_valp` / `tpl_descr.clear_f_val_at_clone_decache` 할당을 `db_private_alloc()`에서 `malloc()`으로 변경
- 해당 필드 해제를 `db_private_free()` 두 번 호출 대신 `free_and_init()`으로 교체 (list_file의 tpl_descr 관련 자료구조는 malloc/free로 관리되어야 하는데 parallel heap scan이 db_private_alloc을 사용해 core가 발생하던 문제 수정)
- 인터럽트 발생 시 조기 반환하던 지점에서 writer_results 정리 로직을 제거 (중복 정리 방지, 정리 책임을 한 곳으로 통일)

## PR body (verbatim)

<http://jira.cubrid.org/browse/CBRD-26365>

### Purpose

core dump를 해결합니다.

### Implementation

mergeable list에서, 에러가 발생했을 경우 child thread에서 아직 write를 수행중인데도 main thread에서 관련 자료구조를 free하는 문제가 있습니다. child thread가 종료된 후에 해당 자료구조 (list_file)을 free하도록 변경합니다.
또한 list_file의 tpl_descr 관련 자료구조들은 malloc() free() 로 관리되지만 parallel heap scan에서 db_private_alloc을 사용하여 core가 발생하는 현상을 해결합니다.
