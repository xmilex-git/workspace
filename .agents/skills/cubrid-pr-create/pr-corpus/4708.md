# PR 4708: [STUDY-97] Draft PR for replace curr_page_watcher in heap_next_internal

- state: CLOSED / base: develop / created: 2023-09-25
- diff: +54 -35, 2 files

## Code changes (summary)

- Added a new `old_page_watcher` field to `HEAP_SCANCACHE` (`heap_file.h`) alongside the existing `page_watcher`
- Initialized/unfixed the new `old_page_watcher` in `heap_scancache_start_internal`, `heap_scancache_force_modify`, `heap_scancache_quick_start_internal`, and `heap_scancache_quick_end` (`heap_file.c`)
- `heap_next_internal()`: commented out the local `curr_page_watcher`/`old_page_watcher` variables and their init calls, replacing their usages throughout the function with `scan_cache->page_watcher` / `scan_cache->old_page_watcher` directly
- Removed/commented several `pgbuf_replace_watcher()` calls that previously copied between local watchers and the scan-cache watcher, since the scan-cache watcher is now used directly
- Draft/experimental exploration PR that was ultimately closed unmerged (design later superseded by PR #4735)

## PR body (verbatim)

http://jira.cubrid.com/browse/STUDY-97

