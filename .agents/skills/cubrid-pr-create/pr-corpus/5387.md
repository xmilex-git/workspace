# PR 5387: [CBRD-24046] Result cache for correlated scalar subquery

- state: MERGED / base: develop / created: 2024-08-07
- diff: +1 -1, 1 files

## Code changes (summary)

- `src/base/system_parameter.c`: removed the `PRM_HIDDEN` flag from the `PRM_ID_MAX_SUBQUERY_CACHE_SIZE` (`max_subquery_cache_size`) parameter definition, changing it from `(PRM_FOR_SERVER | PRM_USER_CHANGE | PRM_SIZE_UNIT | PRM_HIDDEN)` to `(PRM_FOR_SERVER | PRM_USER_CHANGE | PRM_SIZE_UNIT)`.
- Makes `max_subquery_cache_size` visible/documentable to users instead of being an internal-only hidden parameter, per the decision to document it in the manual.

## PR body (verbatim)

http://jira.cubrid.org/browse/CBRD-24046

매뉴얼 수정 과정에서 max_subquery_cache_size 파라미터를 PRM_HIDDEN으로 설정하지 않기로 결정하고, 이 파라미터에 대한 설명을 매뉴얼에 추가하기로 했습니다.
