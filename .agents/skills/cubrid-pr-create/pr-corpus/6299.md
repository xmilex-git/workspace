# PR 6299: [CBRD-26161] [Regression] Core dumped in mspace_free at src/heaplayers/malloc_2_8_3.c:5025

- state: MERGED / base: develop / created: 2025-07-07
- diff: +58 -16, 6 files

## Code changes (summary)

- `px_heap_scan_task.cpp`: `thread_p->tran_index`를 원복하던 `orig_tran_index` 대입 라인 제거 — tran_index가 -1로 리셋된 뒤 temp file을 dequeue해 미할당 메모리에 접근하던 문제를 방지
- `px_list_merger.cpp`: `m_head_list_id->last_vpid.volid != NULL_VOLID`일 때만 `pgbuf_set_dirty()`를 호출하도록 가드 추가 (membuf를 fix한 경우 dirty 처리하지 않음)
- `px_query_executor.cpp`/`.hpp`: `add_task()`가 `bool`을 반환하도록 변경하고 `std::system_error`/`std::exception`을 catch해 실패 시 false 반환; `XASL_LINK_TO_REGU_VARIABLE` 플래그가 설정된 노드는 재귀적으로 parallel executor를 만들지 않도록 제외; `xcache_uses_clones()`가 false면 parallel 실행 자체를 포기하도록 constraint 추가
- `px_query_task.cpp`: 스레드 항목의 `tran_index`를 저장/복원하던 `temp_tran_index` 관련 코드 제거 (worker thread가 더 이상 tran_index를 바꾸지 않으므로)
- `query_executor.c`: `add_task()` 실패(exception) 시 parallel 실행 대신 `qexec_execute_mainblock()`으로 폴백하도록 `qexec_execute_mainblock_internal()`과 `qexec_execute_cte()` 양쪽에 처리 추가
- Regression 테스트에서 발견된 4건의 core dump(잘못된 tran_index 접근, xasl clone 미사용 시 메모리 소유권 문제, membuf dirty 처리, add_task 예외 미처리)를 한 커밋으로 종합 수정

## PR body (verbatim)

<http://jira.cubrid.org/browse/CBRD-26161>
<http://jira.cubrid.org/browse/CBRD-26163>
<http://jira.cubrid.org/browse/CBRD-26164>
<http://jira.cubrid.org/browse/CBRD-26167>

### Purpose

Regression 테스트에서 발생한 4건의 코어 원인을 해결합니다.

### Implementation

1. tran_index가 -1로 리셋된 다음 temp file을 dequeue하여 할당되지 않은 메모리에 접근하는 문제를 해결합니다.
2. XASL clone을 사용하지 않는 상황에서 aptr을 실행한 다음 child thread에서 clear 시 xasl 전부를 clear하여 메모리 소유권 문제가 발생하는 것을 해결합니다. (xasl clone 미 사용시 parallel aptr 불가능하게 하는 제약사항 또한 추가됩니다.)
3. membuf를 fix한 경우 pgbuf_set_dirty를 수행하지 않도록 변경하여 관련 코어 상황 발생을 방지합니다.
4. add_task에서 발생하는 exception을 catch하고, XASL이 regu_var와 link 된 경우 parallel execution을 방지합니다.
