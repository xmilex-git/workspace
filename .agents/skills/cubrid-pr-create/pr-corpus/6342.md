# PR 6342: [CBRD-26210] [Regression] Core dumped in parallel_heap_scan::memory_mapper::memory_mapper at src/query/parallel/px_heap_scan/px_heap_scan_memory_mapper.cpp:555

- state: MERGED / base: develop / created: 2025-07-28
- diff: +0 -13, 1 files

## Code changes (summary)

- Removed a block of `assert_release` calls in `memory_mapper::memory_mapper` (px_heap_scan_memory_mapper.cpp) that hard-coded and asserted exact struct sizes (`sizeof (SCAN_ID) == 2184`, `val_descr == 48`, `heap_cache_attrinfo == 56`, `regu_variable_list_node == 120`, `PRED_EXPR == 48`), including their explanatory comment block.
- Eliminates a core dump triggered whenever these hard-coded struct-size assumptions no longer matched the actual (platform/build-dependent) sizes.

## PR body (verbatim)

<http://jira.cubrid.org/browse/CBRD-26210>

### Purpose

SCAN_ID의 크기를 체크하는 assertion을 제거합니다.
