You are a runtime worker for the CUBRID tooling repo at /home/cubrid/dev/workspace (read its AGENTS.md house rules first: never write scratch to /tmp or $TMPDIR). Scratch: T=<TICKET SCRATCH, e.g. /home/cubrid/dev/workspace/.git_ignored_dir/scratch/312-343> (exists; subdir sa/). Do NOT commit, push, or post anything. Do not edit any source file, test case or probe file. This is lane C of three gate lanes the lead launched at the same time (the others run the whole CTP suite on the optdebug install and the counter harness); do not touch their work.

Delegation execution contract (verbatim; obey strictly):
- Finite gate work (incremental build, unit, smoke — steps that each finish within ~10 min) runs as FOREGROUND blocking commands, chained in one continuous turn. run_in_background/nohup/monitors are FORBIDDEN for such steps. Only a full fresh build may go background, and then the SAME turn must bounded-poll its completion marker (`timeout ... until grep ...`) — never end a turn "waiting for a notification".
- The worker must deliver its final report in the same turn the work finishes, BEFORE going idle.

No CTP in this lane. Do not rebuild anything.

Environment notes: set variables inline (`VAR=x cmd`, never `env VAR=x cmd` — ~/.local/bin/env shadows /usr/bin/env). Use /usr/bin/grep for -E/-P. rm only literal paths (never variable-prefixed globs, never wildcards). Engine worktree W=/home/cubrid/dev/cubrid-worktree/dpin (branch dpin, HEAD <CAND_SHA>; ` m cubrid-cci` is a pre-existing submodule flag — leave the worktree exactly as it is). Installs, both already built from HEAD and read-only here: O=/home/cubrid/dev/workspace/.git_ignored_dir/scratch/dpin/install-optdebug, R=/home/cubrid/dev/workspace/.git_ignored_dir/scratch/dpin/install-release. TC file: TC=<the ticket's new case, e.g. /home/cubrid/dev/cubrid-tc-worktree/dpin/sql/_07_misc/domain_collation_gate/cases/branch_collations.sql>.

Runner: V=/home/cubrid/dev/workspace/tools/fast-gate. fast-sa.sh <out-dir> <label> <install> <sql> [...] runs every job at once, each in its own user+mount+IPC+PID namespace: the install is a volatile overlay (SA logs never land in O or R) and each job creates a fresh database on a volatile overlay (no fsync). Per job it writes <out-dir>/<label>.<sql base>.out / .err (csql stdout/stderr, the names run-probe.sh used), .createdb.log, .rc, .rel. One fresh database per job: the probe files each create and drop their own tables. It refuses to overwrite an existing output, so the labels must be new.

Note on the Bash tool: its checker refuses compound commands that mention a path containing "git" (every path here contains .git_ignored_dir) together with `&`, `wait` or loops. Run each command below as ONE plain command exactly as written.

Steps (foreground, one continuous turn):
1. `date -u +%FT%TZ` (record as START); record `git -C $W status --short` and `git -C $W log --oneline -1`; `ls -la --time-style=full-iso $O/lib/libcubrid.so.11.5 $R/lib/libcubrid.so.11.5`.
2. SA runs, all at once in one call (Bash timeout 600000; well under a minute): `bash $V/fast-sa.sh $T/sa/out <LABEL> $O $TC <LABEL>r $R $TC <LABEL>r $R <PROBE 1, e.g. $T/sa/probe-branch.sql> <LABEL>r $R <PROBE 2, e.g. $T/sa/probe-branch2.sql>; echo rc=$?` — report its full output (per-job table, wall_s).
3. Report, ignoring only lines that differ solely in an elapsed time (say how many you ignored): `diff $T/sa/out/<LABEL>.<TC base>.out $T/sa/out/<LABEL>r.<TC base>.out`; `diff $T/sa/out/<develop reference>.<probe 1 base>.out $T/sa/out/<LABEL>r.<probe 1 base>.out`; the same for probe 2. Report every diff IN FULL. Report the FULL content of $T/sa/out/<LABEL>.<TC base>.out. Then `/usr/bin/grep -n -i -E 'assert|core dumped|signal|-1383|unresolved' $T/sa/out/<LABEL>.*.err $T/sa/out/<LABEL>r.*.err`. For any core these runs dumped (`find /bench/hdd/core -maxdepth 1 -newermt 'START' -name 'core.csql.*'`): `gdb -batch -ex 'set solib-search-path <install>/lib' -ex bt <install>/bin/csql <core> 2>&1 | head -60`, then delete that core by its literal path.
4. Confirm no csql process of yours is left (`pgrep -u $USER -a csql`) and `git -C $W status --short` equals step 1's.

Final report: step 1 record; the fast-sa table and wall_s; the diffs in full; the full optdebug TC output; the grep; cores with backtraces (or "none"); leftover processes; git status check.
