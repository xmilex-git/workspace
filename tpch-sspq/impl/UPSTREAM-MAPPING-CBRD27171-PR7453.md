# 상류 이슈 ↔ 캠페인 IMP 매핑 (2026-08-05)

캠페인 tpch-sspq-impl-r1-20260803. 근거: 프리즌 improvement-registry(blob c3859181…),
triage-adjustments.json의 §5-e upstream_scope_gates, 두 레퍼런스 원문.

## 1. CBRD-27171 — "OR 술어에서 단일 테이블 제약 추출 → 스캔 필터" (오늘 생성, Open)

### 상응 IMP: **IMP-027 (사실상 동일 결함, 1:1 대응)**

| 항목 | CBRD-27171 | IMP-027 (registry) |
|---|---|---|
| 결함 | OR 각 분기에 테이블별 조건 혼재 → OR 전체가 join term으로만 분류, sarg 0개 | "OR of per-relation AND groups yields no single-relation restriction, every base scan unfiltered, join drives entire inner relation" |
| 대표 질의 | TPC-H SF10 Q19 (part×lineitem, 3분기 OR) | Q19 |
| 수정 방향 | 전 분기 공통 테이블 전용 조건을 OR로 묶어 중복 제약으로 추가(필요조건이므로 결과 불변) + 선택도 이중 반영 보정 | 동일 (추출 변환 부재가 근본 원인; 하부 CNF 처리 능력은 이미 존재) |
| 실측 | 5.622s → 0.618s (develop+300버킷+8G에서는 46.9s → 개선폭 더 큼) | 캠페인 기대절감 43.60 s (전 후보 중 최대) |

**주의**: 보고자 sohee jung — 이 티켓은 사실상 IMP-027의 문제 제기가 상류로 올라간 것.
현재 캠페인 큐에서 IMP-027은 5번째이고 §5-e 게이트 **비대상**이었다 (게이트 세트는
CBRD-27127/27036/27037/27094/27113 + PR #7453 → IMP-003/022/019/011/014 한정).

### 필요한 사용자 결정 (§11-a)

CBRD-27171은 아직 티켓만 있고(패치·PR 없음, Triage Waiting) 상류 구현이 시작되지 않았다.
선택지:
- (a) IMP-027에 upstream gate로 CBRD-27171 추가(triage-adjustments 갱신) — 착수 시점에
  상류 진행 상태를 재확인하고, 상류가 구현을 시작했으면 stop-and-report
- (b) IMP-027을 external_tracking 레인으로 이동 — 큐에서 빠지고 추적만 유지 (최대 기대절감
  43.6 s를 상류에 맡기는 결정)
- (c) 현행 유지 — 티켓이 Open일 뿐이므로 캠페인이 먼저 구현·측정 (이 경우에도 결과는 상류
  티켓에 기여 가능)

## 2. PR #7453 — "[CBRD-26746] Use histograms for join selectivity + heap fetch by fanout" (open, 미머지)

### 이미 §5-e 게이트 세트에 포함된 PR이다

triage가 이 PR을 IMP-003·IMP-022·IMP-019·IMP-011·IMP-014 다섯 후보의 **set-to-set**
게이트 레퍼런스로 못박아 두었다(후보별 개별 매핑은 의도적으로 미기록 — "각 후보를 6개
레퍼런스 전부와 대조하라"). 아래는 이번에 PR 본문을 정독한 **내용 기준** 상세 대응:

| PR #7453 구성요소 | 가장 근접한 IMP | 대응 정도 |
|---|---|---|
| ① 조인 선택도에 히스토그램(eqjoinsel_inner 방식, MCV 매칭) | **IMP-014** (다중 컬럼 FK 조인 선택도 과소추정) | 부분 겹침 — 같은 조인 선택도 경로를 교체하지만, IMP-014의 결함(다중 컬럼 곱셈)은 per-column 히스토그램만으로는 완전 해소 미확정. 착수 시 §5-e 대조 필수 |
| ② MCV 채택 기준을 PG 통계검정으로 복원 | **IMP-003·IMP-022** (LIKE/NOT LIKE 선택도가 히스토그램 버킷 경계값만 사용) | 기계 겹침 — 같은 히스토그램·MCV 기반시설을 고치지만 LIKE 패턴 선택도 로직 자체는 별개. 겹침 판단은 착수 시 코드 대조로 |
| ③ 비커버링 인덱스 heap fetch 비용 fanout 반영 (qo_iscan_cost) | **IMP-011** (병렬도-맹목 조인 비용 모델) 인접 | 같은 qo_*cost 영역, 다른 결함. IMP-011의 병렬도 반영과는 독립 |
| ④ #6782 hash join 통합 | 직접 대응 IMP 없음 | 비용 교정과 결합 필요성만 기록 |
| (M-L 반복 프로브 캐시 보정 — PG cost_index 3요소 이식) | IMP-011·IMP-012 인접 | 반복 프로브 비용 정밀화. IMP-012(Q08) 착수 전 참고 가치 |

### 상류 진행 상태 (2026-07-16 기준 최신 코멘트)

- 리뷰어(shparkcubrid) 결정: **①·②는 별도 PR로 분리해 리뷰·반영**, ③은 분석 후 방향 결정.
- 즉 이 PR은 이대로 머지되지 않고 쪼개진다. ①·②의 분리 PR이 나오면 IMP-014·IMP-003·
  IMP-022의 §5-e 대조 대상이 그 새 PR들로 늘어난다.
- TC Merge Gate 차단 중(테스트케이스 PR 2건 draft), JOB 기준 −72.6% 실측 첨부.

## 3. 캠페인에 주는 실질 함의 요약

1. **IMP-027 착수 전 CBRD-27171 재확인이 사실상 의무가 됐다** — 위 (a)/(b)/(c) 결정 필요.
   현재 진행 중인 IMP-005와는 무관하므로 Phase 2는 계속 진행한다.
2. **IMP-014·IMP-003·IMP-022 게이트가 살아있는 표적이 됐다** — #7453이 분리 PR로 진화
   중이므로, 각 후보 착수 시점에 "그 시점의" 분리 PR 목록까지 §5-e 대조에 포함해야 한다.
3. IMP-019(동일 테이블 attr-vs-attr 비교 선택도 0.1 하드코딩)는 #7453 구성요소와 직접
   겹침 없음 — 게이트 대조는 여전히 수행하되 커버될 가능성은 낮다고 판단된다.
4. IMP-011의 핵심(병렬도 반영 비용)은 #7453이 다루지 않는다 — 다만 비용 모델 지형이
   바뀌는 중이므로 착수 시 rebase 대상 코드가 달라질 수 있음을 계획서에 반영할 것.
