CUBRID temp-workmem Phase2 MIGRATE producer migration (#78). Branch wm-integ-7173-develop @660e56d1f, repo /home/cubrid/dev/cubrid-workmem. Authority: SSOT(#75=temp_workmem/issue65_ssot.md) > ADR 0001-0006(temp_workmem/docs/adr) > tape-model.md > issue78_plan.md; terms=CONTEXT.md(PG canonical); evidence=#76/temp_workmem/issue65_evidence.md. Phase1 (1A/1B/1C) + Phase2 infra (chunk_distributor + QFILE_BACKING_KIND tag) already landed/folded into the integration tree.

SHARED CONSTRAINTS (apply to every story):
- Operator-atomic single-backing: one QFILE_LIST_ID is wholly OLD or wholly NEW, never mixed. Switch producer + all its consumers in one commit. coord_type + all OLD machinery RETAINED through #78 (Phase3=#74 deletes).
- Forbidden (SSOT 0 P1-P6 / mandate 2): #62 perf as goal; force membuf==NULL real-VPID connect; raw-fd-single-overflow revival; dirty-flush option-E symptom patch.
- Per-operator gate (mandate 3): robust-parity serial==parallel = COUNT(*)/SUM(CAST col AS NUMERIC(38,0))/MIN/MAX over subquery, incl. terminal/last page. FORBIDDEN: raw-stdout |md5sum, MD5(GROUP_CONCAT) (FAIL-05/08). Parallel actually engaged PROVEN via ;trace on "parallel workers: N>=2" (not in ;plan). serial force = parallelism=1 AND max_parallel_workers=1 BOTH. no-mixed-backing per-list + backing-kind entry guard (production-hard er_set at connect/append + qfile_open_list_sector_scan/sector_page_iterator input + chunk_distributor/tapeset_scan; per-tuple/per-page = debug assert). pgbuf_fixes==0 (hard). CoV<=15%. orphan-zero normal+abnormal. TDE integrity (wmg003). debug no assert/crash/core. unit gate gate_tapeset_scan.sh G1-G15 green maintained.
- Ops (mandate 4): build only via cubrid-build skill (WORKSPACE=/home/cubrid/dev/cubrid-workmem just build release|debug, cwd=/home/cubrid/dev/workspace; raw cmake forbidden). server lifecycle only via /skill:cubrid-server-control (raw cubrid server start/stop hangs on pipe-capture). fresh build PL boot fail -> conf stored_procedure=no. DBs tpch_sf10(60M heavy-spill, release), wmg003(TDE), wmloc(t 4.19M single int). heavy validation on release builds. Never touch m cubrid-cci submodule or untracked bench/harness/results.
- Governance (SSOT 7): each gate evidence APPEND to #76 / temp_workmem/issue65_evidence.md; SSOT fact change -> update #75 (user permission needed for SSOT). commits cite #78 and #73. Re-grep coordinates from HEAD before each edit (R7).

@goal: 2A-0 HARD prereq (R2 concurrent read + overflow + entry guard)
HARD GATE before any live chunk_distributor wiring. Build + unit-verify, no live producer wiring yet. (1) ADR 0005: refactor qfile::buffile::read_page to be re-entrant on caller-supplied cipher/plain scratch (remove member m_stored/m_plain from the read path; pages_read atomic or per-reader); shared fd + pread (offset-stateless); add a per-reader tape view (own page scratch + own TDE-decrypt scratch) resolving logical page N by the same prefix-RAM-vs-read_page(N-prefix) arithmetic; single shared mutable read state = chunk_distributor atomic chunk cursor only; single-reader R1 keeps working as degenerate one-reader view. (2) ADR 0006: overflow-continuation = contiguous page run; start page keeps overflow flag + total size (=> run length); each continuation page records two Tape-relative offsets (first-page offset, run-end) in header fields vacated by old overflow VPID; first-page-owner reassembles forward across chunk boundary (frozen/immutable, pread); a reader landing on a continuation page whose first page is before its chunk skips the whole run in O(1) AND advances the shared chunk cursor past run-end; implement tapeset_scan overflow path (currently asserts false / S_ERROR at qfile_tape.cpp). (3) generalized production-hard backing-kind ENTRY guard (SSOT round-3 (d)/(e)): at every backing-sensitive consume boundary (qfile_connect_list / qfile_append_list / qfile_open_list_sector_scan + sector_page_iterator input / chunk_distributor / tapeset_scan) compare list backing_kind -- OLD mechanism rejects NEW(tapeset) list, NEW mechanism rejects OLD list -- entry boundary = er_set+error (production-hard, release too); combine_two_list exempt (scan-based, IR-8); per-tuple/per-page checks stay debug assert. (4) A~E runtime counter "NEW-backed list touched by OLD scan-bypass path". EXIT GATE: gate_tapeset_scan.sh G1-G15 + NEW tests green (N-reader concurrent view no race incl. TDE wmg003; per-chunk overflow reassembly correct cross-boundary; G16 backing-kind entry guard NEGATIVE rejects NEW-into-OLD & OLD-into-NEW); buffile/scan pgbuf_fixes==0; debug build green; baseline robust-parity reproduced. No live chunk_distributor wiring until this passes.

@goal: 2A-1 SORT final-output single-Tapeset producer atomic switch
Recommended START. Migrate the FINAL merged sort output (all parallel sort types: plain SORT/ORDER BY + #7173 GROUP_BY/ANALYTIC/ORDER+LIMIT, single-final-output model) to NEW backing. Coordinates (re-ground at HEAD first, R7): per-worker output qfile_generate_tuple_into_list(T_SORTKEY) @ list_file.c:4259 -> tape_writer.append_page -> freeze() -> Tape; fan-in sort_merge_run_for_parallel @ external_sort.c:4868 (our connect-free qmgr_append_list_to_single_owner :4937) + sort_run_final_single/sort_merge_worker_runs_to_one -> tapeset import into QFILE_LIST_ID; mark backing_kind=NEW; scan via tapeset_scan; membuf prefix reactivated (tiny=no-spill, no BufFile); qfile_check_no_mixed_backing + backing-kind entry guard live around this operator; ownership via the 1A copy/clear tapeset DEPENDENT_MODE branches. sort_split_input_temp_file already deleted by #7173 (IR-6 dissolved); SORT input = qfile_open_list_sector_scan @ external_sort.c:5376/5531 stays OLD with entry guard. EXIT GATE: SORT/ORDER BY on wmloc + tpch_sf10 heavy-spill PSORT serial==parallel robust parity incl. terminal page (trace-proven parallel workers>=2); backing_kind=NEW on final sort output, OLD elsewhere; no-mixed=0; entry guard fires 0 live (fires on synthetic); A~E counter==0; membuf-on + tiny-no-spill counters; pgbuf_fixes==0; CoV<=15%; PSORT median <= develop x1.10; debug no assert/crash/core; G1-G15 green.

@goal: 2A-2 parallel-scan pre-aggregation (per-worker Tape + chunk_distributor readers)
Migrate parallel-scan pre-aggregation. Coordinates (re-ground at HEAD, R7): pre-sized slot (IR-5, reuse existing structure) px_scan_result_handler.cpp:104-118 (list_id_headers/m_valid/m_list_closed/m_first_vpid/m_last_vpid); pre-agg output qfile_generate_tuple_into_list(T_NORMAL) @ query_aggregate.cpp:2517 (+qfile_add_tuple_to_list :2526/:2738) -> tape_writer; group-by qexec_gby_put_next query_executor.c:5058. Each worker produces its own Tape via tape_writer (pre-sized slot i -> worker i, lock 0); freeze; tapeset imports the per-worker Tapes; parallel downstream readers use chunk_distributor (64-page offset-range, 2A-0 concurrent view + overflow reassembly); backing_kind=NEW; no-mixed + entry guards live. inventory E OLD reader px_scan_input_handler_list.cpp:91 (get_next_page_with_fix, sector-based) BYPASSED on NEW path (not deleted). EXIT GATE: HASHAGG / parallel pre-agg on wmloc + tpch_sf10 serial==parallel robust parity (trace-proven); r2_metrics.cov<=15% on heavy single-Tape skew; pgbuf_fixes==0; no-mixed=0; entry guard 0 live; A~E counter==0; HASHAGG median <= develop x1.10; G1-G15 green; orphan-zero; TDE wmg003.

@goal: 2A-3 hash join partition = per-worker tape import (ADR 0004) + vestigial removal
Migrate hash join. Coordinates (query_hash_join.c stable, #7173 untouched; re-ground at HEAD, R7): each (worker, partition) becomes its own frozen Tape; a partition = a tapeset importing per-worker Tapes; remove per-worker staging + part_mutexes (~:2340) + qfile_append_list copy (:1709/:1764) FOR THE MIGRATED hash join; drop use_connect (:1898, connect :1909) ONLY for NEW-backed inputs (per-PAIR predicate, safe because both contexts belong to one operator); connect_list + membuf==NULL assert remain for OLD until contract; PRIVATE_SPILL membuf-OFF removal scoped to NEW (query_manager.c:3371/3385/3916/3920, QFILE_FLAG_PRIVATE_SPILL query_list.h:793), OLD keeps forced-OFF; parallel partition readers use chunk_distributor; entry guard asserts migrated PHJ never reaches connect/append with a Tapeset operand; backing_kind=NEW. Remove vestigial raw-fd-overflow helpers (dead, call0): px_hash_join_task_manager.cpp:46-120 (get_raw_fd_overflow_page/get_sector_scan_list_id/has_raw_fd_overflow_pages + sector_scan_list_ids map) + px_hash_join.cpp register/unregister_sector_scan_list_id (:87/121/146/527/605) + .hpp. EXIT GATE: PHJ on wmloc + tpch_sf10 serial==parallel robust parity (trace-proven); #77 (parallel PHJ allocate-0 / FAIL-10) RESOLVED on NEW path (HARD; OLD-path crash accepted legacy-until-contract, not patched, optional 1-line diagnostic only); no part_mutexes on migrated path; entry guard 0 live; CoV<=15%; pgbuf_fixes==0; no-mixed=0; A~E counter==0; PHJ median <= develop x1.10; deadlock-free; G1-G15 green; orphan-zero; TDE wmg003.

@goal: 2A-4 Inventory A~E residual sweep (migrated operators -> residual 0)
For every migrated operator confirm zero residual OLD scan-bypass reads reachable on the NEW path, by BOTH static enumeration AND the 2A-0 runtime counter (NEW-backed list touched by OLD bypass == 0). Inventory (re-enumerate at HEAD): A membuf[...] direct ~15 (list_file.c/query_manager.c/temp_page_store.cpp); B membuf==NULL/membuf_last ~3 (query_hash_join.c/scan_manager.c); C sort_split first_vpid/last_vpid range split = DELETED by #7173 (inventory-C residual dissolved, IR-6 fully resolved); D last_pgptr next_vpid direct NULL query_executor.c:10040-10046; E sector px_scan_input_handler_list.cpp ~2 (BYPASSED on NEW, retired at Phase3). EXIT GATE: per-operator residual on NEW path = 0 (static where possible + runtime counter==0); full query suite robust parity (trace-proven); no-mixed=0; entry guard fires 0 live; G1-G15 green.

@goal: 2A-EXIT exhaustive OLD-symbol sweep (Phase3 entry premise)
Produce a checklist artifact under bench/harness/checklists/ classifying every OLD symbol as still-live-for-which-OLD-operator vs fully-dead-after-migration. OLD-symbol baseline (src-wide, re-enumerate at HEAD): qfile_connect_list x7; coord_type x16; dependent_list_id x4; rawfd_find_and_mark_dirty x3 (per-tuple dirty lock=#62 root); QFILE_LIST_SECTOR_SCAN_INFO x21 (grew with #7173 -> Phase3 retire surface); NOT_USE_MEMBUF=0 (already QFILE_FLAG_PRIVATE_SPILL); PRIVATE_SPILL via QFILE_FLAG_PRIVATE_SPILL/PRIVATE_SPILL_FALLBACK (query_manager.c:3371/3920, query_list.h:793). #78 does NOT delete (Phase3=#74). EXIT GATE: every OLD symbol classified; all migrated operators pass full-suite robust parity serial==parallel incl. terminal page (trace-proven); orphan-zero normal+abnormal; debug no assert/crash/core; TDE integrity wmg003; all hard counters green (pgbuf_fixes=0, no-mixed=0, entry guard=0, A~E counter=0, CoV<=15%, membuf-on/tiny-no-spill); G1-G15 green. Sweep artifact reviewed = Phase3 may begin = #78 complete.
