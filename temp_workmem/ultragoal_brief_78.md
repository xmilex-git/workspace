CUBRID temp-workmem redesign Phase2 MIGRATE producer migration (#78), continuing approved plan issue78_plan.md.
Repo /home/cubrid/dev/cubrid-workmem, branch wm-integ-7173-develop. Done before this run: 2A-0 (commits 43083adf8, fc4974dd4), 2A-1a dormant producer hook (commit 7a62d7f60).
Authority order: SSOT (temp_workmem/issue65_ssot.md) > ADR0001-0006 > tape-model.md > plan. Terms = CONTEXT.md (PostgreSQL canonical). NEVER reintroduce §0 P1-P6 contamination (correctness-done/perf-only; dirty-flush opt; force membuf==NULL real-VPID connect; raw-fd-single-overflow revival).
Build: WORKSPACE=/home/cubrid/dev/cubrid-workmem just build debug|release (cwd /home/cubrid/dev/workspace); re-apply `printf '\nstored_procedure=no\n' >> ~/CUBRID/conf/cubrid.conf` after EVERY build. Server lifecycle via /skill:cubrid-server-control wrapper only; stop server before SA selftest. DBs: tpch_sf10 (heavy, release), wmg003 (TDE), wmloc (t 4.19M). NEVER commit/modify `m cubrid-cci` or untracked bench/harness/results.
Per-substep gate (operator-atomic, ALL must pass before next): robust-parity serial==parallel COUNT(*)/SUM(CAST col NUMERIC(38,0))/MIN/MAX over subquery incl terminal page (NO raw-stdout|md5sum, NO MD5(GROUP_CONCAT)); parallel engage proven via `;trace on` `parallel workers: N(>=2)`; serial forced by parallelism=1 AND max_parallel_workers=1; pgbuf_fixes==0 hard; CoV<=15%; orphan-zero; TDE integrity wmg003; debug no assert/crash/core; unit gate gate_tapeset_scan.sh G1-G18 green; no-mixed-backing per-list + production-hard backing-kind entry guard live; heavy DISTINCT/PSORT median <= develop x1.10. Each substep ends: commit (msg cites #78 and #73) -> push -> #78 append (commit hash) -> ledger checkpoint. SSOT (#75) edited only on real fact change with user permission; evidence (#76 / issue65_evidence.md) APPEND each substep.

@goal: 2A-1b SORT engine wiring to NEW backing
Wire the live SORT producer/consumer onto the landed NEW backing (tape_writer/freeze, tapeset/tapeset_scan, buffile, chunk_distributor, backing_kind tag), operator-atomic. Steps: (1) implement ADR0006 overflow-on-NEW producer stamping in list_file.c (replace the two clean-error stubs at qfile_allocate_new_page is_ovf_page branch and qfile_add_tuple_to_list overflow branch) so a NEW-backed producer lays an overflow tuple as a contiguous run of pages (START page TUPLE_COUNT=-2 + OVERFLOW_PAGE_ID=self offset; continuation pages +run_end in LAST_TUPLE_OFFSET) matching the R1/R2 reassembly contract; unit-verify with an overflow-bearing producer selftest. (2) serial sort srlist_id (qfile_open_list @list_file.c:4864) NEW opt-in: attach producer_writer_+scratch, neutralize tfile_vfid so has_old_backing stays false, capture tde_encrypted/algo for the tape_writer; put-func produces -> close -> freeze -> tapeset; verify serial-first robust-parity. (3) parallel worker output_file NEW + fan-in sort_merge_run_for_parallel (external_sort.c:4868) imports each worker frozen tape into srlist_id tapeset instead of qmgr_append_list_to_single_owner (:4937), ownership via copy/clear DEPENDENT_MODE. (4) atomic-switch flag (SORT output only opt-in; the whole sort operator switches together, never mixed). (5) membuf reactivation / tiny-no-spill. (6) heavy robust-parity (wmloc light + tpch PSORT heavy, trace-proven) + pgbuf_fixes=0 + CoV + PSORT median<=develop x1.10 + orphan + TDE wmg003 gates. Re-grep all coordinates at HEAD before each edit (R7 drift). Keep OLD path absolutely unchanged (NEW branch only when producer_writer_!=NULL).

@goal: 2A-2 Parallel-scan pre-aggregation migration
Migrate parallel-scan pre-aggregation: each worker produces its own Tape via tape_writer (pre-sized slot i -> worker i, lock 0; reuse existing writer_results/list_id_headers slot structure at px_scan_result_handler.cpp:104-118); freeze; tapeset imports per-worker Tapes; parallel downstream readers use chunk_distributor (64-page offset-range, the 2A-0 concurrent view + overflow reassembly); pre-agg output qfile_generate_tuple_into_list(T_NORMAL) @query_aggregate.cpp:2517; backing_kind=NEW; no-mixed + backing-kind entry guards live; inventory E OLD readers (px_scan_input_handler_list.cpp sector-based) BYPASSED not deleted on NEW path. Gate: HASHAGG / parallel pre-agg on wmloc + tpch_sf10 serial==parallel robust parity (trace-proven), CoV<=15% on heavy single-Tape skew, pgbuf_fixes=0, no-mixed=0, A~E counter=0, HASHAGG median<=develop x1.10. Re-grep coordinates at HEAD.

@goal: 2A-3 Hash-join per-worker tape import migration
Migrate hash join (ADR 0004): each (worker, partition) becomes its own frozen Tape; a partition = a tapeset importing per-worker Tapes; remove per-worker staging + part_mutexes + qfile_append_list copy for the migrated PHJ (query_hash_join.c part_mutexes ~:2340, append :1709/:1764); drop use_connect ONLY for NEW-backed inputs (per-PAIR predicate :1898); connect_list + membuf==NULL assert remain for OLD until contract; PRIVATE_SPILL membuf-OFF removal scoped to NEW (query_manager.c:3371/3920); parallel partition readers use chunk_distributor; remove vestigial raw-fd-overflow residue (px_hash_join_task_manager.cpp:46-120, px_hash_join.cpp register/unregister_sector_scan_list_id); #77 (FAIL-10 parallel PHJ allocate-0) RESOLVED on the NEW path (HARD line; OLD-path crash accepted legacy-until-contract). Gate: PHJ on wmloc + tpch_sf10 serial==parallel robust parity (trace-proven), #77 resolved on NEW path, no part_mutexes on migrated path, CoV<=15%, pgbuf_fixes=0, no-mixed=0, PHJ median<=develop x1.10, deadlock-free. Re-grep coordinates at HEAD.

@goal: 2A-4 Inventory A~E residual sweep
For every migrated operator confirm zero residual OLD scan-bypass reads reachable on the NEW path, by BOTH static enumeration AND the 2A-0 runtime A~E counter (NEW-backed list touched by an OLD bypass == 0). Inventory (evidence H-3, re-enumerate at HEAD): A membuf[pageid] direct (list_file.c/query_manager.c/temp_page_store.cpp); B membuf==NULL/membuf_last branch (query_hash_join.c/scan_manager.c); C sort_split first_vpid/last_vpid range split (DELETED by #7173 -> inventory-C residual GONE); D last_pgptr next_vpid direct NULL (query_executor.c ~:10040); E sector distribution (px_scan_input_handler_list.cpp) BYPASSED on NEW path (retired at Phase3). Gate: per-operator residual on NEW path = 0 (static + runtime counter==0); full query suite robust parity; no-mixed=0; backing-kind entry guard fires 0 live.

@goal: 2A-EXIT Exhaustive OLD-symbol classification sweep
Produce a checklist artifact (bench/harness/checklists/) enumerating every OLD symbol (qfile_connect_list, coord_type, first_vpid/last_vpid identity, dependent_list_id chain, raw-fd registry, per-tuple dirty lock, QFILE_LIST_SECTOR_SCAN_INFO, NOT_USE_MEMBUF/PRIVATE_SPILL gates) classified still-live-for-which-OLD-operator vs fully-dead-after-migration. Gate: every OLD symbol classified; all migrated operators pass full-suite robust parity serial==parallel incl terminal page; orphan-zero normal+abnormal; debug no assert/crash/core; TDE integrity wmg003; all hard counters green (pgbuf_fixes=0, no-mixed=0, backing-kind guard=0, A~E counter=0, CoV<=15%, membuf-on/tiny-no-spill). Sweep artifact reviewed = Phase3 (#74) may begin. This completes Phase2 (#78).
