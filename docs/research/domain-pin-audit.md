# 실행 하위 삭제 감사표 — S-01~S-43 + collation 쌍 조건 26곳 + S 목록 밖 비교 (D-320-04)

지도: xmilex-git/workspace#312 · 신설: #340(dpin-14, 2026-09-24) · 확정: #344(dpin-18)
지점 정본: `domain-pin-exec-sites.md` §1(S-01~S-43)·§4(collation 26곳) · 삭제 축: `domain-pin-architecture.md` §1.5

## 판정 규칙 (D-320-04)

지점은 ① **정적 근거**(계획·게이트 뒤 조건이 거짓임을 코드로 설명)와 ② 그 티켓의 CTP sql 전수·medium 에서 해당 카운터 0 을 **모두** 만족할 때만 삭제한다. 하나라도 어긋나면 멈추고 티켓에 재계획한다. 카운터가 0 이어도 정적 근거가 서지 않으면(CTP 가 그 경로를 밟지 않았을 뿐) 지우지 않고 셀 TC 를 먼저 더한다. 유지 지점은 optdebug assert 경계와 근거 한 줄을 둔다. "값·행에 따라 의미가 진짜 남는다"는 판정은 규칙표 행과 사용자 승인이 있어야 한다(아래 **X**).

판정 어휘: **삭제**(코드 없음) · **읽기**(자리는 남고 계획·게이트 결정을 읽는다, 값에서 정하지 않음) · **경계**(도달 0 을 optdebug assert + release `ER_QPROC_DOMAIN_UNRESOLVED` -1383 로 지킨다) · **유지**(연산 구현, 결정이 아님) · **X**(승인된 값 의존 의미 — 근거 결정 번호) · **대기**(소유 티켓이 아직 열려 있음).

카운터 증거는 perfmon 카운터(`SHOW EXEC STATISTICS`, 셀 하네스)와 진단 빌드 CTP 전수 집계(각 카운터 증가 자리에 이유별 한 줄을 찍는 빌드, 커밋하지 않음) 두 가지다. CTP 는 perfmon 추적을 켜지 않으므로 전수 증거는 진단 집계와 경계 assert(코어 0)다.

## 1. 지점 S-01~S-43

| ID | 지점(요약) | 판정 | 티켓 | 정적 근거 | 카운터 증거 | 경계 위치 |
|---|---|---|---|---|---|---|
| S-01 | `fetch_peek_arith` 도메인 탈착→값 재확정→원복 | 읽기 · 경계 · X(D-336-E) | #340 | 게이트 의존 노드는 실행의 첫 계산에서 G1 결정을 읽는다(`fetch_arith_gate_reading`). "값 없음" 결정(NULL 피연산자·거부 쌍·병합 불가 collation·VARIABLE 목표 CAST)은 도메인 없이 계산해 NULL 이나 연산자 오류만 낸다. 탈착·재확정·원복은 세션변수 읽기가 결정을 벗어난 뒤의 첫 계산(D-336-E)과 게이트 상태 없는 vd(F-334-01 → #352)에만 남는다 | 진단 전수 `sql-20260924T121845Z-1064659`·`medium-20260924T121845Z-1064660`: 늦은 해석 12줄 전부 `bug_bts_4562`(D-336-E) · 게이트 `sql-20260924T145139Z-1311656` 17462/17462 · `medium-20260924T145139Z-1311657` 975/975 · 카운터 P1/P2/P8 fetch 0(p0·p24) | `fetch_arith_gate_reading` UNRESOLVED → assert + -1383 · "값 없음" 노드의 비NULL 값 → assert + -1383 |
| S-02 | 같은 함수, collation 재확정 | 읽기 · 경계 · X(D-336-E; D-338-02 는 #343 에서 없어짐) | #340 | collation 게이트 노드(#338)의 결정을 읽는다. 게이트가 정하지 못한 노드(D-338-02)와 바뀐 세션변수 읽기 위의 결정만 행이 값에서 정한다(`fetch_row_reads_string_domain`) | 진단 전수 `sql-20260924T121845Z-1064659`·`medium-20260924T121845Z-1064660`: 미결정 15줄·11가지(D-338-02, 목록은 #343 인계), 3줄 `bug_bts_6605`(D-336-E) · 게이트 `sql-20260924T145139Z-1311656` 17462/17462 · `medium-20260924T145139Z-1311657` 975/975 | `fetch_row_reads_string_domain` 거짓 → assert + -1383 |
| S-03 | oracle 빈 문자열 결과 타입 VARIABLE 검사 | 읽기 | #340 | 결과 도메인이 열려 있으면 게이트 결정의 타입으로 검사한다 | — | — |
| S-04 | NVL/COALESCE/NVL2/NULLIF/LEAST/GREATEST 두 값 공통 타입 추론 | 읽기 · 경계 · X(S-01 의 늦은 해석 경로) | #340 | 결정된 노드는 해석기(dpin-07)가 준 공통값 도메인을 읽어 추론 자리에 오지 않는다. "값 없음" 노드는 인자가 모두 NULL 이면 NULL. 추론은 S-01 이 늦은 해석을 남긴 경로에만 | 게이트 `sql-20260924T145139Z-1311656` 17462/17462 · `medium-20260924T145139Z-1311657` 975/975 | `assert (no_value_domain == NULL)` |
| S-05 | `fetch_peek_dbval_slow` VARIABLE/collation 확정 블록 | 읽기 · 경계 · X(S-09 → #352, D-336-E, F-334-01 → #352; D-338-02 는 #343 에서 없어짐) | #340 | 슬롯은 바인드 값 도메인(#336), 값 포인터·위치는 생산자(#337), 문자 함수는 자기 결정(#338)을 읽는다(`fetch_read_plan_domain`, optdebug 그림자 assert: 타입·codeset·collation). 로드가 못 걷던 두 regu(비상관 스칼라 부분질의의 미리 실행 regu, 집계 PERCENTILE 비율)를 걷고, 식 안의 GROUP_CONCAT 누산기 독자는 collation 게이트 노드다. 그림자 assert 가 PX 워커 클론의 누산기 도메인 누수를 잡아 워커가 쓰기 전에 비운다(D-340-09) | 진단 전수 `sql-20260924T121845Z-1064659`·`medium-20260924T121845Z-1064660`: S-09 제자리 6 케이스(#352 인계), D-338-02 4줄(`bug_bts_13782` ELT), 로드 누락 2줄(`issue_11088/11089 _11_subquery`)·GROUP_CONCAT 1줄(`collation_gate_probes`) — 뒤 둘은 게이트 런에서 결정 읽기 · 게이트 `sql-20260924T145139Z-1311656` 17462/17462 · `medium-20260924T145139Z-1311657` 975/975 · PX 동시 세션 30회차 abort 0 | `fetch_read_plan_domain` → assert + -1383 |
| S-06 | REGUVAL_LIST 행 도메인 확정 | 읽기 · 경계 | #340 | 행마다 자기 항목의 결정을 S-05 처럼 읽는다 | 게이트 `sql-20260924T145139Z-1311656` 17462/17462 · `medium-20260924T145139Z-1311657` 975/975 | S-05 와 같음 |
| S-07 | 세션변수 읽기(`T_EVALUATE_VARIABLE`) | 읽기(게이트 노드, #336 S5) · X(D-336-E) | #336·#340 | 읽기가 결정을 벗어나면 그 읽기 비트를 켜고, 그 비트에 기대는 결정만 늦은 해석(읽기별 마스크 `slot_volatile_reads` × `changed_reads`). 표 재조회(D-325-10)는 쓰지 않는다 | 진단 전수 `sql-20260924T121845Z-1064659`·`medium-20260924T121845Z-1064660`: 29줄 전부 `bug_bts_4562` · 카운터 셀 [N-sv-change] 2, 그 밖 0 | — |
| S-08 | `qdata_*_dbval` 값 타입 dispatch | 유지 | — | 연산 구현(결정 아님) | — | — |
| S-09 | `eval_value_rel_cmp` rhs 제자리 coerce | 삭제 | #352 | 비교 항마다 비교 기록(D-352-01): 로드 또는 G1 이 방향·목표·collation·실패 결과를 정하고, 상수 쪽은 G1 이 자기 값으로 1회 변환하거나 복사한다(공유 값은 그대로). 제자리 coerce 와 그 힙 전환 코드는 없다. 공유 바인드를 제자리에서 바꾸던 다른 읽기 두 곳(키 상한 리밋·ORDER BY LIMIT, F-352-14)도 지역 값으로 | 진단 전수 `sql-20260925T013259Z-2171250`·`medium-20260925T013259Z-2171251`(C2): 기록 있는 항의 develop 비교 0 · C3 진단 전수 `sql-20260925T021813Z-2287455`·`medium-20260925T021813Z-2287454`: 경계·그림자 불일치 0 · 게이트 `sql-20260925T023041Z-2398071` 17465/17465 · `medium-20260925T023041Z-2398072` 975/975(고정 배치, 코어 0) · 카운터 P1·P9-range-heap `Num_domain_coerce_compare` 0(p0·p24) | 기록 없는 XASL 항·게이트 상태 없는 게이트 칸 → `eval_value_rel_cmp` 경계 (b) assert + -1383 |
| S-10 | `tp_value_compare_with_error (do_coercion=1)` 술어 호출 | 읽기 · 경계 · X(D-336-E; D-338-02 는 #343, S-42 의 비교와 set/list 내부는 #354 에서 없어짐) | #352 · #354 | 비교 kernel(DIRECT·CONVERT·COLLATIONS·OBJECT)을 기록에서 읽는다(D-352-02). IN/SOME/ALL 원소 비교도 계획한다(D-352-03): 리스트 열·스칼라 = 비교 기록, 상수 집합 = G1 이 원소 위치마다 결정·1회 변환, 행이 계산하는 컬렉션 = 원소 키 표(집합 함수는 피연산자 키, 집합 속성은 원소 도메인, 그 밖은 전 키, D-352-07). develop 비교는 지도 예외 D-336-E(세션변수)에만 남고 셈이 붙는다. 게이트 미결정 D-338-02 는 #343 이, 술어 스트림(S-42)과 set/list 비교 내부는 #354 가 없앴다(스트림 로드 기록·키 쌍 표, §3) | C3 진단 전수 `sql-20260925T021813Z-2287455`·`medium-20260925T021813Z-2287454`: 남은 develop 비교 22줄 전부 술어 스트림(filtered index 술어, S-42), 경계·그림자 불일치 0 · 게이트 `sql-20260925T023041Z-2398071` 17465/17465 · `medium-20260925T023041Z-2398072` 975/975(고정 배치, 코어 0) · 카운터 P1·P9-range-heap·P7 `Num_domain_coerce_compare` 0, P6-in(IN 부분질의) 9,955,000 → 0 · `Num_planned_convert` 9,955,000(행 × 리스트 값 × 변환 1); P6-cte·P6-in L·P9-range-index 에 남은 셈은 B-tree 키 비교(`btree_compare_key`·`pr_midxkey_compare_element`, 셀마다 `Num_domain_key_coerce` 와 같음 → #342) · #354 종료 진단 `sql-20260926T020322Z-112727`·`medium-20260926T020322Z-112726`: 남은 develop 비교 0줄 · 게이트 `sql-20260926T023234Z-247586` 17470/17470 · `medium-20260926T023234Z-247585` 975/975(고정 배치, 코어 0) | `eval_value_rel_cmp`: 사유 NULL/OPEN/UNPLANNED 에서 develop 이 값으로 정할 것이 있으면(형·문자열 collation 이 다름) assert + -1383 |
| S-11 | `qexec_topn_cmpval` VARIABLE 폴백 | 읽기 · X(D-336-E) | #340 | 정렬 키마다 계획 도메인을 top-N 준비 때 1회 읽어 `cmpval` 을 직접 부른다(`qexec_topn_sort_domains`). 세션변수 읽기에 기대는 키는 값이 도메인을 벗어난 비교에서만 값 타입으로 비교 | 카운터 P4-topn `Num_domain_coerce_compare` 실행당 1,004,600 → 1(남은 1 = LIMIT 절 `qexec_check_limit_clause`, #352 인계) → #352 C2 뒤 0(`t352-c2`·`t352-c3` 카운터) | — |
| S-12 | `btree_compare_key` 비교 불가 폴백 | 읽기 · 경계 · 유지(계획 밖 검색) · X(D-336-E; D-338-02 는 #343 에서 없어짐 — 규칙 없는 DECIDED 원소의 값은 경계) | #342 | 인덱스 스캔의 검색 키 비교는 스캔의 키 비교 표를 읽는다(`BTID_INT.search_keys` → `domain_search_key_compare`, D-342-05): 단일 컬럼 폴백, 복합 키 원소 훅(`pr_midxkey_compare_planned`), range 정렬·중복 제거·병합. 표는 로드(원소가 모두 로드 고정일 때) 또는 G1 8단계(상수·결정 원소가 있을 때)가 키 컬럼마다 만나는 키 쌍 전부로 만든다(D-342-03). 단일 컬럼 키는 값 그대로라(F-342-01) 폴백 자리는 남고 비교는 계획에서 온다. 값이 결정을 벗어난 컬럼(세션변수 D-336-E, 게이트가 정하지 못한 문자열 D-338-02)의 스캔만 develop 비교(`values_decide`, 셈). 계획 밖 B-tree 검색(인덱스 스캔 식별자 없는 고유성·외래 키·잠금 검색)은 인덱스 자기 키끼리라 develop 폴백 그대로 | 진단 전수 `sql-20260925T133317Z-3516866`·`medium-20260925T133317Z-3516867`(착수): 키 셈 두 결함 — 병렬 인덱스 스캔 승격이 키 계획 저장소를 놓아 워커 비교가 해제된 기억을 읽음(`cbrd_26906`, develop 비교 6,400,205줄), OBJECT 결정 도메인과 OID 값(권한 카탈로그 `_db_auth` 검색, 345 케이스 602줄) — 둘 다 이 티켓에서 고침(F-342-03·04) · 진단 전수 `sql-20260925T141432Z-3628714`·`medium-20260925T141432Z-3628713`(후보): 키 셈 0줄(지도 예외 줄도 0), 경계·그림자 불일치 0, 키 경로의 develop 비교는 loose index scan 의 optdebug 진행 확인 1줄(최종 커밋에서 계획 비교, F-342-05) · 게이트 `sql-20260925T143127Z-3734285` 17468/17468 · `medium-20260925T143127Z-3734284` 975/975(고정 배치, 코어 0) | 표에 없는 쌍 → `domain_search_key_compare` assert + -1383 |
| S-13 | `qfile_update_domains_on_type_list` | 삭제 · X(D-336-E; D-338-02 는 #343 에서 없어짐) | #341 | 함수 없음. 리스트는 열 때 계획 도메인으로 타입을 받는다(S-14). 첫 튜플이 타입을 주는 열은 두 가지 — 세션변수 읽기 위 결정(D-336-E), 게이트가 정하지 못한 문자열(D-338-02) — 뿐이고 셈이 붙는다. 가지 도메인이 다른 집합 연산·CTE 열은 게이트가 실행 전에 ER_QPROC_INCOMPATIBLE_TYPES 로 거부한다(#341 후속, 사용자 결정 D-341-14 — develop 은 두 가지 모두 행이 있을 때만 거부하고 한쪽이 비면 다른 쪽 타입을 썼다). NULL 피연산자 위 LEAD/LAG 는 예외가 아니다: 기본값이 함수의 NULL·열린 도메인으로 바뀌지 못해 develop 처럼 ER_TP_CANT_COERCE 로 끝나므로 NULL 만 담는다(#341 후속, F-341-05)(`qexec_type_open_list_columns`, PX 기록자 포함). 값 없는 결정의 생산자 열은 NULL 만 담으므로 NULL 도메인(D-341-01) | 진단 전수 `sql-20260925T061904Z-2933005`·`medium-20260925T061904Z-2933004`(23770a1e7): 23줄 전부 D-336-E(`bug_bts_4562` 22·`fetch_gate_counters` 1) — 착수 163줄 · 게이트 `sql-20260925T041739Z-2611254` 17465/17465 · `medium-20260925T041739Z-2611256` 975/975(커밋 A) · 게이트 `sql-20260925T111359Z-3384089` · `medium-20260925T111359Z-3384088`(후속 `31c17f1fc`, 고정 배치, 코어 0) | 열 수 없는 열 → `qdata_get_valptr_type_list` assert + -1383 |
| S-14 | `qdata_get_valptr_type_list` | 읽기 · 경계 | #341 | vd 를 받아 열린 열에 게이트 결정·로드 고정 도메인을 준다(`qexec_consumer_domain`, D-341-02). 행이 주는 열(S-13 의 세 가지)만 컴파일 도메인으로 열린다 | 게이트 `sql-20260925T041739Z-2611254` 17465/17465 · `medium-20260925T041739Z-2611256` 975/975(커밋 A) · `sql-20260925T055519Z-2824080` 17465/17465 · `medium-20260925T055519Z-2824079` 975/975(커밋 B, 고정 배치, 코어 0) | 계획 답 없음 → assert + -1383 |
| S-15 | `qfile_unify_types` VARIABLE 채택·-1509 | 읽기 · (D-338-02 는 #343 에서 없어짐 — -1509 분기는 행이 타입을 주는 가지에만) | #341 | 튜플 없는 리스트의 VARIABLE 채택 대신, 빈 쪽은 값을 주지 않으므로 다른 쪽 도메인을 막지 않는다(`list1_empty` — 행이 타입을 주는 가지(D-336-E·D-338-02)에만 닿는다; 게이트가 정한 도메인이 다른 가지는 실행 전에 거부된다, #341 후속 D-341-14). collation -1509 분기는 D-338-02 로 남는다 | 게이트 `sql-20260925T041739Z-2611254` 17465/17465 · `medium-20260925T041739Z-2611256` 975/975(커밋 A) | — |
| S-16 | `qfile_initialize_sort_key_info` cmpdisk 폴백 | 삭제 | #341 | 키 도메인은 계획의 것(행이 주는 키는 값이 해결한 열의 것, S-17). 값이 해결하지 못한 키는 NULL 만 비교한다 | 게이트 `sql-20260925T041739Z-2611254` 17465/17465 · `medium-20260925T041739Z-2611256` 975/975(커밋 A) | — |
| S-17 | `qexec_resolve_domains_on_sort_list` | 삭제 → 읽기 · 경계 · X(D-336-E, #337; D-338-02 는 #343 에서 없어짐) | #341 | `qexec_plan_sort_list_domains`: ORDER BY·DISTINCT·분석·GROUP BY 키와 집합 연산 자체 정렬(통합된 리스트를 읽는다)의 키가 계획 도메인. 행이 주는 키만 열 regu 도메인(셈) | 진단 전수 `sql-20260925T061904Z-2933005`: 20줄 전부 D-336-E(`bug_bts_4562`) — 착수 25줄 · 게이트 `sql-20260925T041739Z-2611254` 17465/17465 · `medium-20260925T041739Z-2611256` 975/975(커밋 A) | 계획 답 없음 → assert + -1383 |
| S-18 | `qexec_resolve_domains_for_group_by` | 삭제 → 읽기 · 경계 · X(D-336-E; D-338-02 는 #343 에서 없어짐) | #341 | `qexec_plan_group_by_domains`: GROUP BY 키·정렬 리스트 위치·해시 키·출력 열이 계획 도메인, 집계는 스캔 전에 셋업(S-23). 위치의 답 없음은 경계, 행이 주는 위치는 열 regu 도메인(셈). `qexec_finish_group_by_domains` 는 NULL 만 본 누산기의 NULL 도메인과 해시 부분 리스트 타입(계획을 읽은 키)만 채운다. #340 인계 4 의 GROUP BY GROUP_CONCAT 칸(`domain_fetch_gate`)이 develop 답 | 진단 전수 `sql-20260925T061904Z-2933005`: 0줄 — 착수 S-18 11줄·해시 키 16줄·NULL 누산기 450줄 · 게이트 `sql-20260925T055519Z-2824080` 17465/17465 · `medium-20260925T055519Z-2824079` 975/975(커밋 B, 고정 배치, 코어 0) | 위치 답 없음 → assert + -1383 |
| S-19 | 분석 `resolve_domain:` 블록 | 읽기 · 경계 · X(D-336-E; D-338-02 는 #343 에서 없어짐) | #341 | 위치가 계획이 정한 열을 읽는다. 행이 주는 열만 정렬 리스트 첫 튜플이 준 타입(셈) | 진단 전수 `sql-20260925T061904Z-2933005`: 0줄 — 착수 13줄 · 게이트 `sql-20260925T041739Z-2611254` 17465/17465 · `medium-20260925T041739Z-2611256` 975/975(커밋 A) | 계획 답 없음 → assert + -1383 |
| S-20 | `resolve_domains_on_list_scan` | 삭제 → 읽기 · 경계 | #341 | 리스트 스캔 위치가 계획을 읽는다(`scan_plan_list_scan_domains`); `resolve_domain_on_regu_operand` 도 없다. 행이 주는 위치만 리스트 타입(셈) | 진단 전수 `sql-20260925T061904Z-2933005`: 0줄 · 게이트 `sql-20260925T041739Z-2611254` 17465/17465 · `medium-20260925T041739Z-2611256` 975/975(커밋 A) | 계획 답 없음 → assert + -1383 |
| S-21 | connect-by 프로브 NUMERIC p/s 보정 | 삭제(로드 고정) | #341 | START WITH … CONNECT BY 프로브 도메인은 로드가 컴파일 도메인만으로 고정한다(`domain_fix_connect_by_probe`); 실행마다 새 도메인 복사 없음 | 게이트 `sql-20260925T041739Z-2611254` 17465/17465 · `medium-20260925T041739Z-2611256` 975/975(커밋 A) | — |
| S-22 | 해시 조인 조인 키 공통 타입 | 읽기 | #341 | 키 도메인은 두 리스트의 계획 도메인; 키당 카운터 제거 | 게이트 `sql-20260925T041739Z-2611254` 17465/17465 · `medium-20260925T041739Z-2611256` 975/975(커밋 A) | — |
| S-23 | `qexec_resolve_domains_for_aggregation` | 삭제 · 경계 · X(D-336-E; D-338-02 는 #343 에서 없어짐) | #341 | 블록의 모든 집계는 첫 행 전에 계획에서 셋업한다(`qexec_setup_aggregate_domains`, 전부-아니면-전무 폐기, D-341-04): GROUPBY_NUM·BIT/문자 열 MEDIAN·분류 못한 MEDIAN 값·PX 워커 클론도. 값 없는 결정의 함수는 NULL 만 보므로 누산기를 비워 둔다. 첫 값에 남는 일(`qexec_aggregate_first_values`): 문자 열·식 MEDIAN/PERCENTILE 의 DOUBLE 캐스트 확인(첫 실패 값 -1118, 뒤 값 -181 — D-335-10 답; 게이트가 정하지 못한 문자열 식도 DOUBLE 은 셋업이 정한다, #341 후속), 게이트가 분류하지 못한 리터럴·바인드의 -1118(첫 non-NULL 값, 분류 없이 — #341 후속), 세션변수 읽기 값의 분류 확인(내용이 바꾼 분류, `qexec_interpolation_class_holds`, D-341-08), 행이 정하는 집계(세션변수 읽기가 결정을 벗어났거나 시작 때 값이 없던 것, 미결정 문자열)의 해석기 규칙 도메인 — 이 마지막 둘만 셈 | 진단 전수 `sql-20260925T061904Z-2933005`·`medium-20260925T061904Z-2933004`: 집계 셈 자리 0줄(분류 변경·분류 없음·행이 정하는 집계), 경계 0 — 착수 S-23 546줄·첫 행 재호출 781줄 · 게이트 `sql-20260925T055519Z-2824080` 17465/17465 · `medium-20260925T055519Z-2824079` 975/975(커밋 B, 고정 배치, 코어 0) · 카운터 t341-b2: `Num_domain_resolve_list`·`Num_domain_resolve_agg` 전 셀 0(p0·p24), P7-agg-24·P7-scan-24 6 → 0 | 셋업 답 없음 → assert + -1383 |
| S-24 | BUILDVALUE 출력 regu 도메인 | 읽기 | #341 | 누산기를 읽는 출력 열은 셋업(첫 행 전)에서 계획이 정한 함수 도메인을 받는다(`qexec_type_accumulator_outputs`, #341 후속 — D-341-09 의 블록 끝 1회를 대신한다); 행마다 복사하지 않는다. 행이 정하는 집계(D-336-E·D-338-02)의 출력 열만 결정이 날 때 받는다 — 직렬은 첫 값, PX 리더는 워커 결정을 병합할 때(D-338-02; 세션변수 읽기는 PX 스캔을 막는다). 값은 그 도메인으로 변환된다 — GROUP_CONCAT 의 CHAR 바인드는 VARCHAR 값을 만든다(#340 인계 4, F-341-04) | 진단 `sql-20260925T032501Z-2504581`·`medium-20260925T032501Z-2504582`(착수, 54d94c147): 237줄 전부 계획 결정과 같음(same=1) · 게이트 `sql-20260925T055519Z-2824080` 17465/17465 · `medium-20260925T055519Z-2824079` 975/975(커밋 B, 고정 배치, 코어 0) · 게이트 `sql-20260925T102709Z-3164422` · `medium-20260925T102709Z-3164421`(후속 `0428320f8`, 고정 배치, 코어 0) | — |
| S-25 | `qdata_finalize_aggregate_list` 정렬 키 | 읽기 | #341 | 키는 리스트 열의 도메인(GROUP_CONCAT 등은 셋업이 인자 도메인으로 정한 키, MEDIAN/PERCENTILE 은 셋업이 리스트 도메인과 함께 정한 키 — `qexec_setup_interpolation_list`, #341 후속) | 게이트 `sql-20260925T055519Z-2824080` 17465/17465 · `medium-20260925T055519Z-2824079` 975/975(커밋 B, 고정 배치, 코어 0) · 게이트 `sql-20260925T102709Z-3164422` · `medium-20260925T102709Z-3164421`(후속 `0428320f8`, 고정 배치, 코어 0) | — |
| S-26 | `qdata_update_agg_interpolation_func_value_and_domain` | 유지(변환) · 경계 | #341 | 늦은 바인딩과 DOUBLE·DATETIME·TIME 캐스케이드 없음: 값을 함수 도메인으로 1회 변환한다(D-341-05). 리스트 도메인은 셋업이 정한다(`qexec_setup_interpolation_list`, #341 후속 — D-341-10 개정): 인자 도메인이 함수 도메인과 같은 타입이면 인자 도메인(같은 타입·다른 정밀도의 NUMERIC 값, F-341-03), 아니면 함수 도메인 — develop 의 리스트가 첫 값에서 받던 최종 상태다. 리스트는 처음부터 그 도메인으로 열리고(직렬·PX, `qdata_aggregate_list_domain`) 값이 리스트 타입을 바꾸지 않는다; 세션변수의 분류가 문장 안에서 바뀌면(D-336-E) 키와 열린 리스트가 따른다. 상수·호스트 변수 인자(정렬 리스트 없음)는 리스트가 없고 값 하나를 셋업의 분류로 1회 변환한다(`qdata_evaluate_aggregate_list` — 첫 값 DOUBLE·DATETIME·TIME 캐스케이드와 도메인 갱신 삭제, 감사표 밖이던 자리, F-341-06) | 게이트 `sql-20260925T055519Z-2824080` 17465/17465 · `medium-20260925T055519Z-2824079` 975/975(커밋 B, 고정 배치, 코어 0) · 게이트 `sql-20260925T102709Z-3164422` · `medium-20260925T102709Z-3164421`(후속 `0428320f8`, 고정 배치, 코어 0) | 함수 도메인이 분류 밖, 리스트 타입 ≠ 함수 타입 → assert + -1383 |
| S-27 | 분석 첫값 도메인 블록 | 읽기 · X(D-336-E; D-338-02 는 #343 에서 없어짐) | #341 | 게이트 결정을 읽는다; MEDIAN/PERCENTILE 의 세션변수 값은 내용이 분류를 바꾸면 develop 바인딩(D-341-08). 분류 못한 값은 첫 실행 블록(S-28)이 develop 처럼 거부한다. 행이 정하는 함수만 셈. DISTINCT 의 리스트 도메인 변환은 결정이 아니므로 셈 제거 | 진단 전수 `sql-20260925T061904Z-2933005`: 0줄 — 착수 S-27 3줄·S-28 12줄 · 게이트 `sql-20260925T055519Z-2824080` 17465/17465 · `medium-20260925T055519Z-2824079` 975/975(커밋 B, 고정 배치, 코어 0) | — |
| S-28 | 분석 보간 첫값 switch | 읽기 · X(D-336-E) | #341 | 열린 함수는 게이트 분류, 문자 열·식은 컴파일 DOUBLE. 분류 못한 값·NULL 값은 develop 과 같은 캐스트로 거부·분류(셈 없음); 행이 정하는 함수만 셈 | 진단 전수 `sql-20260925T061904Z-2933005`: 0줄 — 착수 S-27 3줄·S-28 12줄 · 게이트 `sql-20260925T055519Z-2824080` 17465/17465 · `medium-20260925T055519Z-2824079` 975/975(커밋 B, 고정 배치, 코어 0) | — |
| S-29 | `qdata_analytic_is_plain_sum_avg` 차단 조건 | 읽기 | #341 | 게이트 결정이 있으면 빠른 경로; 행이 정하는 도메인은 일반 경로가 읽고 센다(차단 셈 제거) | 게이트 `sql-20260925T055519Z-2824080` 17465/17465 · `medium-20260925T055519Z-2824079` 975/975(커밋 B, 고정 배치, 코어 0) | — |
| S-30 | `scan_dbvals_to_midxkey` strict 시도·setdomain | 삭제 → 읽기 · 경계 · X(D-336-E; D-338-02 는 #343 에서 없어짐) | #342 | 원소마다 계획의 규칙을 읽는다(D-342-02): 로드가 고정한 원소는 계획된 strict 변환기를 돌려 인덱스 컬럼 도메인과 원소 도메인 중 하나를 고르고(INDEX·STRICT·KEEP), 상수(리터럴·바인드·상수 부분트리)는 G1 이 1회 변환하거나 유지한 값(CONSTANT, `Num_domain_gate_convert`), 세션변수 읽기·게이트를 기다리는 도메인·값을 고정하지 않는 도메인의 원소는 G1 이 그 도메인에서 정한 규칙(DECIDED). 값에 대한 strict 시도·정확 일치 검사·`need_new_setdomain`·`tp_domain_copy`·`retry`·`prebuilt_midxkey_domains` 는 없다. 혼합 키 도메인은 스캔이 open 에 만든 스크래치 체인(구조체 복사, 컬럼 선택이 같으면 재사용 — 할당·캐시·결정 0, D-342-04) 또는 상수 bound 의 G1 도메인. 단일 컬럼 키는 값 그대로(F-342-01, #321 §4.2). 병렬 인덱스 스캔은 승격 뒤에도 리더의 키 계획 저장소를 쓴다(F-342-03) | 진단 전수 `sql-20260925T133317Z-3516866`·`medium-20260925T133317Z-3516867`(착수): 키 셈 두 결함 — 병렬 인덱스 스캔 승격이 키 계획 저장소를 놓아 워커 비교가 해제된 기억을 읽음(`cbrd_26906`, develop 비교 6,400,205줄), OBJECT 결정 도메인과 OID 값(권한 카탈로그 `_db_auth` 검색, 345 케이스 602줄) — 둘 다 이 티켓에서 고침(F-342-03·04) · 진단 전수 `sql-20260925T141432Z-3628714`·`medium-20260925T141432Z-3628713`(후보): 키 셈 0줄(지도 예외 줄도 0), 경계·그림자 불일치 0, 키 경로의 develop 비교는 loose index scan 의 optdebug 진행 확인 1줄(최종 커밋에서 계획 비교, F-342-05) · 게이트 `sql-20260925T143127Z-3734285` 17468/17468 · `medium-20260925T143127Z-3734284` 975/975(고정 배치, 코어 0) · 카운터 t342-c1: P5(eq·between·multi·correlated·ISS·MRO)·P6-cte·P6-in·P9-range-index 의 `Num_domain_key_coerce`·`Num_domain_coerce_compare` 0(p0·p24), `Num_domain_gate_convert` 증가 = 상수 키 원소 수, 결과 행 동일 | 로드 고정 원소의 값이 계획 도메인과 다름 → `scan_key_column` assert + -1383 · 키 계획 없는 스캔·루트 헤더와 다른 키 도메인 → `scan_open_index_key_plan` assert + -1383 |
| S-31 | ISS `last_key` 도메인 | 삭제 | #342 | 건너뛰기 값은 인덱스에서 읽은 첫 컬럼 값이고 계획에서 그 원소 규칙은 INDEX 다(fetch 범위 bound, D-342-02). 값마다 regu 도메인을 쓰지 않는다 — `regu->type = TYPE_DBVAL` 과 값 복제는 그대로 | 게이트 `sql-20260925T143127Z-3734285` 17468/17468 · `medium-20260925T143127Z-3734284` 975/975(고정 배치, 코어 0) · 카운터 P5 ISS `Num_domain_key_coerce` 0 | — |
| S-32 | MRO `tp_Null_domain` 시딩 | 삭제 | #342 | 정렬 컬럼 도메인은 스캔 키 계획의 `asc_key_type`(인덱스 키 도메인의 오름차순 사본)에서 정렬 컬럼마다 1회(L-44, D-342-06). `has_null_domain` 과 그 루프는 없다 | 게이트 `sql-20260925T143127Z-3734285` 17468/17468 · `medium-20260925T143127Z-3734284` 975/975(고정 배치, 코어 0) · 카운터 P5 MRO `Num_domain_key_coerce` 0 | 키 계획 없는 스캔 → `btree_range_opt_check_add_index_key` assert + -1383 |
| S-33 | keylimit NUMERIC assert(계획 도메인 신뢰) | 유지 | #342 | 불변식이 보호한다: 키 리밋 값은 계획 도메인으로 읽은 지역 값이고(#352, F-352-14) NUMERIC assert 가 그 불변식을 지킨다 | 게이트 `sql-20260925T143127Z-3734285` 17468/17468 · `medium-20260925T143127Z-3734284` 975/975(고정 배치, 코어 0) | 기존 assert |
| S-34 | PX 워커 집계 도메인 resolve | 삭제 | #341 (S-23 의 parallel 변형과 함께) | 워커는 상속한 결정으로 클론을 첫 행 전에 셋업한다(`qexec_setup_parallel_aggregates`); 첫 값 단계는 리더와 같다(`qexec_parallel_aggregate_first_values`). 워커마다의 agg-expr 마킹도 없다(D-341-07) | 게이트 `sql-20260925T055519Z-2824080` 17465/17465 · `medium-20260925T055519Z-2824079` 975/975(커밋 B, 고정 배치, 코어 0) · 카운터 P7-agg-24/P7-scan-24 `Num_domain_resolve_agg`·`Num_domain_px_resolve` 6 → 0 | — |
| S-35 | PX 루트 역전파 | 삭제 | #343 | 리더는 스캔 전에, 워커는 상속한 결정으로 첫 행 전에 집계를 셋업한다(`qexec_setup_aggregate_domains`·`qexec_setup_parallel_aggregates`, #341). 워커가 행에서 정하던 집계는 D-338-02 뿐이었고(세션변수 읽기는 PX 가 막는다, `px_scan_checker`) #343 이 없앴으므로 워커가 넘길 도메인이 없다. 리더의 수집 뒤 첫 값 단계(`qexec_parallel_aggregate_first_values`, 워커 표본값 `dbvals_for_domain_resolve`)는 남는다 — 문자 MEDIAN/PERCENTILE 첫 값과 분류 못한 리터럴·바인드의 -1118(develop 답)만 확인하고 도메인을 정하지 않는다 | 게이트 `sql-20260925T172458Z-3882109` · `medium-20260925T172458Z-3882110`(고정 배치, 코어 0) · 카운터 t343-c1: `Num_domain_px_resolve` 전 셀 0(p0·p24) | 병합 자리 `assert` (리더 VARIABLE·워커 결정 조합 없음) |
| S-36 | PX 행당 누산기 폴백 | 삭제 → 경계 | #343 | 셋업이 값을 보는 모든 함수에 첫 행 전에 누산기 도메인을 준다(COUNT·COUNT_STAR BIGINT, MIN/MAX/BIT/GROUP_CONCAT 함수 도메인, SUM/AVG 해석기 누산기, STDDEV/VARIANCE DOUBLE, JSON 집계 JSON — `qexec_setup_aggregate_domains`·`qexec_setup_aggregate_accumulators`). 도메인이 없는 함수는 NULL 만 보고(값 없는 결정) NULL 은 누산 전에 걸러진다 — 폴백 아홉 곳은 닿지 않는다 | 게이트 `sql-20260925T172458Z-3882109` · `medium-20260925T172458Z-3882110`(고정 배치, 코어 0) · 카운터 t343-c1: `Num_domain_px_resolve` 0 | `accumulator_domain_unresolved` → assert + -1383 |
| S-37 | `update_domains_on_type_list_by_val_list` | 삭제 → 경계 | #343 | 함수와 두 호출 자리가 없다. XASL_SNAPSHOT 리스트는 val_list 도메인(`valp->dom`, 로드 `stream_to_xasl.c` 와 스포너만 쓴다)으로 열리므로 그 함수가 넣던 값은 열 때의 것과 같았다 — 튜플마다의 재대입과 닫을 때의 원자 재저장도 없다(열 때 1회 저장만). MERGEABLE_LIST 워커 리스트는 열 때 계획 도메인으로 타입을 받고(`qdata_get_valptr_type_list`) 첫 튜플 타입 지정(`qexec_type_open_list_columns`, `is_list_id_domain_resolved`)이 없다 — 행이 타입을 주는 열은 세션변수 읽기(D-336-E)뿐인데 PX 가 막는다 | 게이트 `sql-20260925T172458Z-3882109` · `medium-20260925T172458Z-3882110`(고정 배치, 코어 0) | 워커 리스트 열기 `list_columns_unresolved` → assert + -1383 |
| S-38 | `qexec_clear_*` 원복 + `original_domain` 필드 | 대기 | [dpin-17c #355](https://github.com/xmilex-git/workspace/issues/355) (#343 범위 분리: 실행이 결정을 노드 필드에 쓰는 동안은 원복이 필요하다) | | | |
| S-39 | 힙 전환(qe 쪽 · qx 집계 쪽) | 삭제 | #352(qe) · #341(qx) | qe: S-09 와 함께 없어졌다(상수는 G1 의 자기 값). qx: MEDIAN 첫 값의 캐스트는 전용 값에 한다 — 공유 바인드·캐시 열 값을 제자리에서 바꾸지 않음, `REGU_VARIABLE_CLEAR_AT_CLONE_DECACHE` 힙 전환 없음(#352 인계) | S-09 와 같음 · 게이트 `sql-20260925T055519Z-2824080` 17465/17465 · `medium-20260925T055519Z-2824079` 975/975(커밋 B, 고정 배치, 코어 0) | — |
| S-40 | `db_to_char` 결과 도메인(INSERT 기본식) | 읽기(타입) | #343 | 포맷은 스키마가 든 리터럴이고 `db_make_string` 이 만든다 — 시스템 codeset·collation 의 VARCHAR. 그 타입이 결과 도메인이다(`qexec_default_format_domain`, 내용과 무관 — 로드가 리터럴을 타입으로 정하는 것과 같다). 값에서 도메인을 얻지 않는다 | 게이트 `sql-20260925T172458Z-3882109` · `medium-20260925T172458Z-3882110`(고정 배치, 코어 0) · optdebug 그림자 assert(값의 도메인과 같음) 0 — 기본식 TO_CHAR 를 쓰는 CTP 21 케이스(`issue_20399_default_ext` 16 등) | optdebug 그림자 assert |
| S-41 | PL/CSQL 바인드 선언 타입 | 해당 없음 | #339 | PL `?` = 사용자 `?`(D-339-01) | — | — |
| S-42 | 필터·함수 인덱스 스트림 | 경계(로드) · 읽기(비교: 로드 기록) | #343 · #354 | `fpcache_claim` 이 언팩 오류를 삼키지 않고 돌려준다(호출자 `locator_sr.c` 는 이미 오류를 처리하고 성공 때 술어를 assert 한다). 스트림 언팩이 GATE regu 를 보면 로드를 거부한다(`stx_index_stream_rejected`, -1383 "load") — 게이트 없이 로드·평가되는 스트림이라 결정할 곳이 없다. 카탈로그 스트림에는 호스트 변수가 없어 닿지 않는다. 술어 스트림 안의 비교는 #354 부터 로드가 기록을 만든다(`domain_plan_stream_compares`). 두 리터럴의 비교는 값으로 정하고, 그 밖의 쪽은 키 쌍 표를 읽는다(§3). `DOMAIN_REASON_PRED_STREAM` 은 없어졌다 | 게이트 `sql-20260925T172458Z-3882109` · `medium-20260925T172458Z-3882110`(고정 배치, 코어 0) — 필터/함수 인덱스 디렉터리 4개 통과 · #354 게이트 `sql-20260926T023234Z-247586` 17470/17470 · `medium-20260926T023234Z-247585` 975/975(고정 배치, 코어 0)(필터 인덱스 술어, NULLIF·GREATEST 함수 인덱스: TC `compare_outside_terms`) | 로드 거부 -1383 · 기록 없는 항 → 경계 (b) |
| S-43 | `hostvar_late_binding` 파라미터 | 대기 | #344 | | | |

## 2. collation 쌍 조건 26곳 (`VARIABLE || collation_flag != NORMAL`)

26곳의 삭제는 [dpin-17c #355](https://github.com/xmilex-git/workspace/issues/355)의 몫이다(#343 범위 분리, 2026-09-26 — 쌍 조건은 실행이 결정을 노드 필드에 쓰는 동안 "이 실행에서 아직 열려 있음"을 가르므로 플랜 불변과 함께 지운다). #343 은 조건 안의 D-338-02 행 읽기를 없앴다 — 게이트가 모든 문자열을 정한다(D-343-01). 아래 "이 티켓 자리" 는 조건을 지우지 않고 그 안의 값 판정만 바꾼 티켓이다.

| # | 자리 | 부류 | 이 티켓 자리 | 판정 | 티켓 |
|---|---|---|---|---|---|
| C-01 | fe:4479 (S-02) | 연산자 결과 | #340 — 조건 안의 값 판정은 결정 읽기 | 대기 | #355 |
| C-02 | fe:5226 (S-05) | 연산자 결과 | #340 — 조건 안의 값 판정은 결정 읽기 | 대기 | #355 |
| C-03 | fe:5239 (S-06) | REGUVAL_LIST | #340 — 조건 안의 값 판정은 결정 읽기 | 대기 | #355 |
| C-04 | lf:7082 (S-13) | 리스트 컬럼 | #341 — S-13 함수 삭제; 쌍 조건은 행이 주는 열의 검사로 `qexec_type_open_list_columns` 에 | 대기 | #355 |
| C-05 | qx:1362 (S-24) | 리스트 컬럼 | #341 — 행마다가 아니라 셋업에서 1회(`qexec_type_accumulator_outputs`), 계획의 함수 도메인을 준다; 행이 정하는 집계만 결정이 날 때(#341 후속) | 대기 | #355 |
| C-06 | qx:21193 (S-17) | 위치 서술자 | #341 — `qexec_plan_sort_list_domains`: 조건은 `qexec_consumer_domain` 의 컴파일 도메인 검사, 안은 계획 읽기 | 대기 | #355 |
| C-07 | qx:21249 (S-18) | 위치 서술자 | #341 — `qexec_plan_group_by_domains` 의 위치·해시 키·출력 열 검사, 안은 계획 읽기 | 대기 | #355 |
| C-08 | qx:21277 (S-18) | 위치 서술자 | #341 — S-18 함수와 함께 삭제(참조 열 도메인 검사) | 삭제 | #341 |
| C-09 | qx:21405 (S-18) | 위치 서술자 | #341 — `qexec_plan_group_by_domains` 의 출력 열 검사(C-07 과 한 루프) | 대기 | #355 |
| C-10 | qx:23137 (S-19) | 위치 서술자 | #341 — 조건 안의 판정은 계획 읽기 | 대기 | #355 |
| C-11 | qx:27787 (S-11) | 정렬 키 | #340 — 계획 도메인 읽기 | 대기 | #355 |
| C-12 | sm:8231 (S-20) | 리스트 스캔 | #341 — `scan_plan_list_scan_domains` 의 위치 검사, 안은 계획 읽기 | 대기 | #355 |
| C-13 | sm:8254 (S-20) | 리스트 스캔 | #341 — C-12 와 한 루프(술어·나머지 regu 리스트) | 대기 | #355 |
| C-14 | sm:8287 (S-20) | 리스트 스캔 | #341 — `resolve_domains_on_list_scan` 과 함께 삭제(술어 피연산자는 비교 기록을 읽는다, #352) | 삭제 | #341 |
| C-15 | sm:8293 (S-20) | 리스트 스캔 | #341 — C-14 와 같음 | 삭제 | #341 |
| C-16 | qx:21328 (S-18) | 집계 | #341 — S-18 함수와 함께 삭제(집계는 스캔 전 셋업, S-23) | 삭제 | #341 |
| C-17 | qx:21336 (S-18) | 집계 | #341 — C-16 과 같음 | 삭제 | #341 |
| C-18 | qx:21440 (S-18) | 집계 | #341 — C-16 과 같음 | 삭제 | #341 |
| C-19 | qx:21605 (S-23) | 집계 | #341 — S-23 함수 삭제; 같은 조건은 게이트 적용(`qexec_apply_aggregate_gate_domain`)과 행이 정하는 집계(`qexec_aggregate_row_first_value`)에 | 대기 | #355 |
| C-20 | qa:1876 (S-25) | 집계 | #341 — 조건 안은 리스트 열 도메인 읽기 | 대기 | #355 |
| C-21 | qa:3343 (S-26) | 집계 | #341 — 늦은 바인딩 삭제, 함수 도메인의 분류·collation 경계 검사로 | 삭제 | #341 |
| C-22 | qn:59 (S-29) | 분석·빠른 경로 차단 | #341 — 조건 안은 게이트 결정 읽기(차단 셈 제거) | 대기 | #355 |
| C-23 | qn:188 (S-27) | 분석 | #341 — 조건 안은 게이트 결정; 행이 정하는 함수만 develop 바인딩(셈) | 대기 | #355 |
| C-24 | sm:8240 (S-20 하위) | 집계 | #341 — `resolve_domain_on_regu_operand` 와 함께 삭제 | 삭제 | #341 |
| C-25 | sm:8264 (S-20 하위) | 집계 | #341 — C-24 와 같음 | 삭제 | #341 |
| C-26 | fe:5273 (S-05 FAST_PEEK) | 빠른 경로 차단 | #340 — S-05 가 결정을 읽은 뒤 빠른 경로가 켜진다 | 대기 | #355 |

좌표(fe/qx/lf/sm/qa/qn)는 `domain-pin-exec-sites.md` 작성 기준(develop `cad27172b`)이다. 지금 소스의 줄 번호는 다르다.

## 3. S 목록 밖 비교 (#354, dpin-17b)

S 번호가 없는 develop 비교, 곧 `tp_value_compare_with_error` 가 타입이 다른 두 값을 받아 `Num_domain_coerce_compare` 를 올리던 호출자다. 착수 진단 전수(a7f2f1ea1 의 진단 빌드, `sql-20260926T010104Z-4064435`·`medium-20260926T010104Z-4064434`)에서 이 셈은 2,151줄이었다. 줄마다 호출 스택을 기호화해 호출자를 가렸다.

판정은 두 가지다. 두 쪽의 키를 플랜이 알면 **비교 기록**(#352 D-352-01 과 같은 부품)을 쓴다. 로드나 G1 이 행 전에 한 번 정하고, 행은 kernel 만 돈다. 키를 데이터만 알면 **키 쌍 표**를 쓴다. 값이 가질 수 있는 모든 키 쌍의 비교를 행 전에 정해 둔 표이고(D-352-07 의 모든 키 규칙), 행은 두 값의 키로 칸을 찾아 읽기만 한다(D-354-01). 컬렉션 원소, JSON 스칼라, 파티션 경계, 해시 그룹 키, 스트림의 열 쪽이 여기에 든다.

| 호출자 | 판정 | 정적 근거 | 착수 진단 줄 | 경계 위치 |
|---|---|---|---|---|
| `col_find`·`col_bsearch`·`col_value_compare`(`col_sort` qsort)·`setobj_compare`·`setobj_compare_order`·`setobj_difference`·`setobj_union`·`setobj_intersection`·`setobj_issome`·`setobj_coerce`·`setvobj_compare` | 읽기(키 쌍 표) | 원소 타입은 컬렉션의 데이터다. 이종 집합, 집합 함수(F-352-18), 디스크에서 다시 읽어 정렬하며 쌓는 집합(`or_get_set`→`col_add`), 캐스트·ALTER(`setobj_coerce`)가 그렇다. 호출자(디스크 복원·정렬·힙·캐스트·술어·집합 연산) 어디에도 플랜이 없다. 서버와 SA 의 `col_element_compare` 가 `domain_compare_by_keys` 를 읽는다 | 1,831 | 표에 없는 키 → optdebug assert(표를 못 만든 경우만 develop, 셈) |
| eval set/list 내부(`eval_item_card_set`·`eval_item_card_sort_list`·`eval_sub_*`, 옛 사유 `DOMAIN_REASON_COLLECTION`) | 읽기(키 쌍 표, kernel KEYS) | 같은 부류다. 사유는 없어졌다 | 0 | 같음 |
| 술어 kernel OBJECT(`eval_compare_planned`) | 읽기(키 쌍 표) | 서버에서 OBJECT 쪽 값은 OID 다. 계획 키가 OBJECT 인데 값이 다른 타입이면(SP 결과, `_08_javasp/418-4`) 두 값의 키로 비교한다 | 1 | 같음 |
| `mr_cmpval_json` | 읽기(키 쌍 표) | 스칼라 키는 `db_convert_json_into_scalar` 가 주는 VARCHAR(시스템 collation)·INT·BIGINT·DOUBLE 이다. 문서 내용이 정한다 | 48 | 같음 |
| `partition_prune_range`·`partition_do_regu_variables_match`·`partition_prune_list`(→`set_ismember`) | 읽기(키 쌍 표) | 경계 값은 파티션 식의 타입이고, 이 타입은 플랜이 아니라 카탈로그(`_db_partition`)가 준다. 새 스트림 항목은 D-318-01 이 `INDX_INFO.key_type` 하나로 막았다. 비교는 스캔 open 마다 한 번이고 행마다가 아니다 | 53 + 18(목록 경계의 `col_find`) | 같음 |
| `qdata_agg_hkey_compare` | 읽기(키 쌍 표, 강제 변환 없음) | 스캔에서 만든 키와 부분 리스트에서 다시 읽은 키를 비교한다. 출처가 둘이라 타입이 다를 수 있다(#341 인계 5) | 0 | 같음 |
| `fetch_peek_arith` FIELD·NULLIF, `db_least_or_greatest`(LEAST·GREATEST) | 읽기(비교 기록) | 로드가 산술 노드마다 비교 기록을 싣는다(`ARITH_TYPE.domain_compare[2]`, 스트림 불변). 두 쪽이 로드에서 정해지면 로드가 kernel 을 고르고, 게이트가 정하는 쪽이 있으면 G1 이 한 번 고른다. 상수 쪽은 G1 이 한 번 변환한다. 상수 부분트리를 한쪽으로 가진 자리는 그 부분트리가 값을 가지는 즉시 정한다(D-354-08) | 75 + 36 | `eval_compare_values_planned` 경계 (b) |
| `qexec_get_orderbynum_upper_bound`(두 ORDERBY_NUM 항의 상한끼리) | 읽기(키 쌍 표) | 상한은 각 항의 오른쪽 값이 가진 타입이고, AND 는 더 작은 쪽을 남긴다. 그래서 위 단계로 올라가는 상한의 키는 값이 정한다. 실행마다 한 번이며 행과 무관하다 | 0 (TC `compare_outside_terms` 의 `for orderby_num () <= 2.5 and orderby_num () < 3`) | 같음 |
| `qexec_check_limit_clause` | 읽기(비교 기록) | row count 와 INT 0 을 비교하는 기록이다(`xasl_node.limit_compare`). 리터럴 쪽은 G1 이 변환한다 | 13 | 같음 |
| `parallel_scan::resolve_instnum_limit` | 읽기(비교 기록) | instnum 항 자신의 비교 기록(inst_num() 대 한계)을 다시 읽는다 | 7 | 같음 |
| `qexec_cmp_tpl_vals_merge` | 읽기(비교 기록) | 머지 열 쌍마다 outer·inner 리스트 열 항목으로 기록을 만든다(`mergelist_proc_node.merge_compares`) | 6 | 같음 |
| 술어 스트림(필터·함수 인덱스, 파티션 식; S-42 의 비교 쪽, 옛 사유 `DOMAIN_REASON_PRED_STREAM`) | 읽기(스트림 로드 기록) | 스트림 로드(`domain_plan_stream_compares`)가 비교 항·ALL/SOME 항·FIELD/NULLIF/LEAST/GREATEST 에 기록을 만든다. 두 리터럴의 비교는 값으로 정하고, 그 밖의 쪽은 키 쌍 표(kernel KEYS)를 읽는다. 스트림의 도메인이 값을 설명하지 않을 수 있어서다. 카탈로그는 컬럼 타입을 바꾼 ALTER 뒤에도 옛 타입으로 컴파일한 필터 술어를 그대로 둔다(F-354-02, `filtered_index_basicfunction_delete_004`). GATE regu 가 든 스트림은 로드가 거부한다(S-42) | 63 | 기록 없는 항 → 경계 (b) |

합계 2,151줄 = 1,831 + 1 + 48 + 71 + 111 + 13 + 7 + 6 + 63(ORDERBY_NUM 0).

**남은 develop 비교.** S 목록 밖에서 develop `tp_value_compare` 를 그대로 부르는 자리는 두 값의 타입이 같게 만들어지는 자리이거나, 다른 티켓이 계획을 준 자리다. 착수 진단에서도 0줄이었다.
- 한 열의 두 값: rollup 그룹 경계(`qexec_groupby` 의 `list_dbvals` 대 현재 튜플, 같은 regu 열 도메인), 분석 함수 그룹 경계(`current_values` 대 `temp_values`, 같은 정렬 열), 클래스 계층 MIN/MAX(`qdata_evaluate_aggregate_hierarchy`, 하위 클래스가 물려받은 같은 속성), 인덱스 통계 키(`btree.c` 의 `pkeys` 대 다음 키 원소, 같은 키 열).
- 한 도메인으로 맞춘 해시 키: 해시 리스트 스캔 키(`qdata_hscan_key_compare`, 빌드 키를 표에 넣을 때 프로브 regu 도메인으로 바꾼다 — 아래 인계), 해시 조인 키(`query_hash_join.c`, 두 키를 같은 `domains[]` 로 읽는다, #341 셋업이 키 도메인을 정한다), `mht_compare_dbvalues_are_equal`(`memory_hash.c`).
- 다른 티켓의 몫: top-N 정렬 키의 D-336-E 가지(`qexec_topn_cmpval`, 셈), 키 변환이 실패한 뒤 develop 의 순위 답(`scan_manager.c`, #342).

**비교가 아닌 행 시점 변환(인계).** 해시 리스트 스캔은 빌드 키를 해시 표에 복사할 때(`qdata_copy_hscan_key`·`qdata_copy_hscan_key_without_alloc`) 값의 타입이 프로브 regu 의 타입과 다르면 그 도메인으로 강제 변환한다(`vtype1 != vtype2` → `tp_value_coerce`). 값의 타입을 보고 행에서 변환을 정하는 자리이지만 비교가 아니라서 `Num_domain_coerce_compare` 에 잡히지 않고, S 목록에도 없다. #354 의 범위(비교) 밖이라 [dpin-17d #356](https://github.com/xmilex-git/workspace/issues/356) 으로 넘긴다.

**vd 없는 fetch(#341 인계 6).** 분석 PERCENTILE 비율(query_analytic.cpp 두 곳, query_executor.c 한 곳)은 실행의 vd 로 읽는다. 로드는 분석 비율 regu 를 걷는다. 보간 임시 regu(`qdata_get_interpolation_function_result`)는 타입이 정해진 리스트 열 도메인을 읽는다. 셋업이 그 리스트를 계획 도메인으로 열었고, D-336-E 열은 첫 튜플이 타입을 주었다. 그래서 fetch 가 도메인을 정할 일이 없다. `fetch_arith_gate_reading` 과 `fetch_row_reads_string_domain` 의 `vd == NULL` 가지는 경계 (b)(UNRESOLVED / false)가 됐다. 착수 진단 전수에서 vd 없는 fetch 셈은 0줄이었다.

**종료 진단.** 같은 계측을 #354 트리에 얹은 전수(`sql-20260926T020322Z-112727` 17469/17469 · `medium-20260926T020322Z-112726` 975/975, 코어 0)에서 `Num_domain_coerce_compare` 셈은 0줄이다. D-336-E 줄도 0줄이다. 키 쌍 표와 비교 기록의 그림자 불일치·경계 assert 도 0건이다. 게이트: `sql-20260926T023234Z-247586` 17470/17470 · `medium-20260926T023234Z-247585` 975/975(고정 배치, 코어 0).
