---
status: proposed
date: 2026-09-22
---
# 도메인·collation 결정 지점은 컴파일과 실행 게이트 두 곳뿐이다 (플랜 불변, PX 워커 결정 0)

질의의 도메인·collation 은 **컴파일(클라이언트 파서 → XASL 스트림)** 과 **실행 게이트 G1(`qexec_execute_query` 가 값 배열을 만든 직후, 실행당 1회)** 두 곳에서만 정해지고, 그 뒤 fetch·비교·집계·정렬·키 range·리스트 스캔·PX 워커 어디에서도 값을 보고 도메인을 추론·보정·복원하지 않는다. 컴파일은 기존 도메인 필드를 전부 확정 값으로 채우고(`DB_TYPE_VARIABLE`·`TP_DOMAIN_COLL_LEAVE` 0) 게이트 확정 슬롯만 `regu->flags` 비트 하나로 표시하며, 슬롯 ID·피연산자 부류·변환기·게이트 의존 노드 목록은 스트림을 바꾸지 않고 **로드 시 1회 도출**한다(`stx_map_stream_to_xasl` 안, 세 로드 경로 공통). 실행별 답은 XASL_STATE 의 게이트 표에만 있고 플랜 노드에는 쓰지 않으며, PX 워커는 `qexec_deep_copy_xasl_state` 하나로 값 배열과 게이트 표를 깊은 복사해 상속만 한다. 결정 원문: #312 D-M3, #317 D-317-02·17·18, #318 D-318-01~08, 정본 `docs/research/domain-pin-architecture.md` §1.

## Considered Options

- **명시 계획 표를 스트림 새 섹션 + 노드마다 `plan_idx` 로 팩**(전략 B): 기각. regu/arith/pred 스트림 레이아웃은 필터 predicate·함수 인덱스 식으로 카탈로그에 저장되는 **디스크 포맷**이라 마이그레이션 없이는 바꿀 수 없고, 도메인의 진실이 노드 필드와 표 두 곳이 된다(P6). B 의 "덤프로 검사 가능" 장점은 로드 도출 결과를 `SHOW PLAN`/trace 에 출력해 얻는다.
- **서버 게이트가 실행마다 트리 전체에 타입 패스**(전략 C): 기각. 정적 계획에도 매 실행 O(트리) 비용, 파서 격자의 서버 복제, prepare 응답 컬럼 메타 불가 — 이전 캠페인 "잔여 확정" 이 실패한 모양(L-40·L-41).
- **플랜 캐시를 바인드 타입 시그니처로 변형**(D): 지도 범위 밖(D-M3 와 상충).
- **게이트를 스캔 open 마다**: 기각. 결정이 블록마다 반복된다. 블록 게이트 G2 는 상관 부류에 계획된 변환기를 적용할 뿐 결정하지 않는다(D-318-08).

## Consequences

- `original_domain`/`original_opr_dbtype` 필드와 클론 원복 5곳·PX 스폰 원본 저장 3곳·워커→루트 역전파가 통째로 사라진다(G-02·G-06). 그 자리는 로드 도출 결과가 재사용한다(구조체 크기 불변, MEM-02).
- `pt_make_regu_hostvar` 의 2단계(바인드 값 타입을 도메인으로)와 바인드 피크 재계획의 값 타입 도메인은 삭제된다 — 클라이언트에 숨어 있던 세 번째 결정 지점(D-318-05). 재계획은 값을 비용 추정에만 쓴다.
- 삭제한 실행 하위 지점(S-01~S-43)마다 optdebug assert + release **신설 오류 코드**를 둔다. `ER_QPROC_INVALID_XASLNODE` 재사용은 기각 — 클라이언트가 그 코드를 받으면 조용히 재컴파일·재실행해 위반을 숨긴다(db_vdb.c:2277, D-318-04).
- 실행 결정 잔존 X(`median(varchar_col)`)는 처음부터 경계 예외 목록에 둔다(D-317-15).
